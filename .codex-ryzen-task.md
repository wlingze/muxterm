# Muxterm 重构任务 — 在 Ryzen 上继续

## 当前状态
- 代码在 `~/Developer/self/muxterm/`，分支 `feat/model-refactor`
- Step 1 已完成（核心 trait + 纯模型 + 测试，297 passed）
- Step 2 半成品（terminal_model.rs 存在但未完成）
- 编译缓存: `~/Developer/self/muxterm-target/`

## 紧急任务: 拆分 Cargo.toml 依赖

当前问题: `gtk4` 和 `vte4` 是硬依赖，导致没有装 GTK 的机器无法编译。
Ryzen 不需要 GTK，只跑 TUI。需要用 Cargo feature flag 拆分:

### Cargo.toml 目标结构

```toml
[features]
default = ["gtk"]     # 默认编译 GTK 版本
gtk = ["dep:gtk4", "dep:vte4"]
tui = ["dep:crossterm"]  # TUI 前端

[dependencies]
# 核心依赖（始终需要）
vte = "0.13"          # ANSI 解析（纯 Rust，无系统依赖）
toml = "0.8"
futures = "0.3"
tokio = { version = "1", features = ["full"] }
anyhow = "1"
thiserror = "1"
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
clap = { version = "4", features = ["derive"] }
async-channel = "2"
async-trait = "0.1"
async-ssh2-tokio = "0.13"
portable-pty = "0.8"
libc = "0.2"

# 可选: GTK4 前端
gtk4 = { version = "0.9", optional = true }
vte4 = { version = "0.8", optional = true }

# 可选: TUI 前端
crossterm = { version = "0.28", optional = true }
```

### 代码改动

1. `src/main.rs` — 根据 feature flag 选择启动哪个前端
   - `#[cfg(feature = "gtk")]` → 启动 GTK 前端
   - `#[cfg(feature = "tui")]` → 启动 TUI 前端
   - 都没有 → 报错

2. `src/platform/linux/` 所有文件 — 加 `#[cfg(feature = "gtk")]`
   - 这样不启用 gtk feature 时这些文件不编译

3. `src/core/` — 不加任何 cfg，核心层始终编译

### 验证

```bash
# 不带 GTK 编译（Ryzen 上用）
cargo build --no-default-features --features tui
cargo test --no-default-features --features tui

# 带 GTK 编译（archmini 上用）
cargo build
cargo test
```

## 重构任务: 继续 Step 2-6

完成依赖拆分后，继续之前的 6 步重构:

- Step 2: TerminalModel 纯逻辑 + mock backend 测试
- Step 3: LocalBackend 从现有代码提取
- Step 4: TmuxBackend 从现有代码提取
- Step 5: ASCII TUI 前端 + --tui flag
- Step 6: GTK4 前端适配 + 全量回归

## 约束
- 不要因为现有代码束手束脚，这是大重构
- 现有 270+ 测试必须保留并回归通过
- commit 信息: feat: / fix: / test: / refactor: / chore:
- 不要加 Co-authored-by