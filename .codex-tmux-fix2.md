# TmuxBackend 修复：pane 过滤 + tab 切换 + 嵌套布局 + pty 输出

## 测试发现的问题

### Bug 1: pane 混乱 — tab1 和 tab2 的 pane 混在一起显示
- 现象：连接 2 tab 3 pane 的 tmux session，TUI 显示 5 个 pane 并排（两个 tab 的 pane 混了）
- 根因：TmuxBackend 没有正确按 tab(=tmux window) 过滤 pane，所有 pane 都塞到 active tab 的 layout 里
- 期望：只显示当前 active tab 的 pane（tab1 显示 3 个，tab2 显示 1 个）

### Bug 2: Alt+数字切 tab 不工作
- 现象：Alt+1 没切到 tab1，tab 栏还是显示 2:tmux
- 根因：TmuxBackend 的 window id（tmux @N）和 muxterm WindowId 不匹配。Alt+1 发 SwitchWindow{WindowId(1)}，但 TmuxBackend 的 window id 是 tmux 的 @0/@1，不是 1/2
- 期望：Alt+数字能切换到对应的 tmux window（tab）

### Bug 3: 嵌套布局没渲染 — tmux layout 字符串没正确转成 LayoutNode
- 现象：3 pane 嵌套分割（左1右上下2）显示成 5 pane 平铺
- 根因：tmux 的 layout 字符串（如 1268,140x30,0,0{70x30,0,0,0,69x30,71,0[69x15,71,0,1,69x14,71,16,2]}）没有正确解析成 LayoutNode 树
- 期望：正确解析 tmux layout 字符串为 Split(H, leaf, Split(V, leaf, leaf)) 结构

### Bug 4: pty 输出不显示 — %output 没同步到 pane_output
- 现象：TUI 内容区空白，shell 输出不显示
- 根因：TmuxBackend 收到 %output @N content 通知后，没有把内容写入 pane 的 output 缓冲
- 期望：%output 的内容累积到对应 pane 的 pane_output，TUI refresh() 时能拉到

## 修复要求

### 1. 修 Bug 1: pane 按 tab 过滤
- State::panes(tab_id) 必须只返回该 tab 内的 pane
- TmuxBackend 的 pane 数据要正确关联到 tab（tmux window）
- layout tree 也只包含当前 tab 的 pane

### 2. 修 Bug 2: window/tab 切换
- TmuxBackend 需要建立 tmux window id → muxterm WindowId/TabId 的映射
- SwitchWindow / SwitchTab 要发正确的 tmux 命令（select-window）
- Alt+数字切 tab 要工作

### 3. 修 Bug 3: tmux layout 字符串解析
- 实现 tmux layout 字符串解析器，转成 LayoutNode 树
- 格式：checksum,WxH,X,Y{left,WxH,X,Y,pane_id,right,WxH,X,Y[upper,...,lower,...]}
- {} = horizontal split, [] = vertical split
- 纯函数，方便单元测试

### 4. 修 Bug 4: %output 同步
- TmuxBackend::handle_message 处理 %output 时，把解码后的 content 追加到 pane 的 output 缓冲
- 推 StateChange::PaneOutput 事件

## 测试要求

### 正向测试
- 2 tab 3 pane 的 tmux session，muxterm 连接后：
  - list-panes 在 tab1 返回 3 个 pane
  - list-panes 在 tab2 返回 1 个 pane
  - list-layout 在 tab1 返回嵌套 Split(H, Split(V)) 树
  - Alt+1/Alt+2 能切 tab
  - pty 输出能显示

### 反向测试
- 连接不存在的 socket → 错误处理
- 连接空 session（刚创建没 pane）→ 不 panic
- kill-session 后再连接 → 错误处理
- tmux session 被外部 kill → muxterm 检测断连

### 边界情况
- 单 pane session（无分割）
- 只有 1 个 tab
- 水平分割后不垂直分割
- pane 被关闭后 layout 正确更新
- 多层嵌套分割（3 层以上）

## 约束
- cargo test --no-default-features --features tui 必须全过
- cargo fmt
- cargo clippy --no-default-features --features tui -- -D warnings 通过
- commit 信息 fix(tmux): / test(tmux):
- 不要加 Co-authored-by
