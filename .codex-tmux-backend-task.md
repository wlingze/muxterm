# TmuxBackend 完善：支持 attach/detach + 完整 pane/window 同步

## 背景
LocalBackend (本地 shell 模式) 已完成：2 tab 3 pane 嵌套分割、daemon session、CLI 命令全部工作。

现在需要完善 TmuxBackend（tmux 控制模式）。当前状态：
- TmuxBackend 能 spawn tmux -CC 并连接
- 但 pane 信息不完整（连接已有 tmux session 时显示 "no pane"）
- attach-session CLI 命令未实现（返回 None）
- tmux 的 window/pane/layout 变更通知没有完全同步到 TerminalModel state

## 目标
1. muxterm 通过 -L <socket> 连接到已有 tmux session 时，能正确显示所有 window/tab/pane
2. 支持 attach / detach 操作（类似 tmux 的 attach/detach 语义）
3. 嵌套分割布局（2 tab 3 pane：tab1 有左1右上下2，tab2 有1 pane）正确同步和显示

## 测试场景（必须通过）

### 场景 1: 连接已有 tmux session
```bash
# 1. 用原生 tmux 创建 2 tab 3 pane 布局
tmux -L test1 new-session -d -s demo -x 140 -y 30
tmux -L test1 split-window -h -t demo           # 水平分割 → 2 panes
tmux -L test1 select-pane -t demo:0.1
tmux -L test1 split-window -v -t demo:0.1       # 垂直分割右侧 → 3 panes
tmux -L test1 new-window -t demo                 # 新建 tab2

# 2. muxterm CLI 连接并查询
muxterm -L test1 list-sessions    # 应看到 demo session
muxterm -L test1 list-windows     # 应看到 2 windows
muxterm -L test1 list-panes       # 应看到 3 panes (在 active tab)
muxterm -L test1 list-layout      # 应看到嵌套分割树

# 3. muxterm TUI 连接
muxterm --tui -L test1            # TUI 应显示 2 tabs，tab1 有 3 panes 嵌套布局
```

### 场景 2: attach / detach
```bash
# attach 到已有 session
muxterm attach-session -t demo -L test1

# 在 muxterm 中操作后 detach
muxterm detach -L test1

# session 持续存在，重新 attach
muxterm attach-session -t demo -L test1
```

### 场景 3: 通过 muxterm 修改 tmux 布局
```bash
muxterm -L test1 new-session -s work
muxterm -L test1 split-pane -h -s work
muxterm -L test1 split-pane -v -s work
# 验证原生 tmux 能看到这些变更
tmux -L test1 list-panes -t work
```

## 实现要求

### 1. 完善 TmuxBackend 的 tmux -CC 事件解析
- 确保 %output / %layout-change / %window-add / %pane-changed 等通知完整解析
- tmux 的 layout 字符串要正确转换成 LayoutNode 树
- window 和 pane 的创建/删除/切换要同步到 state

### 2. 实现 attach-session / detach CLI 命令
- attach-session -t <name>：连接到已有 tmux session（不是 new）
- detach：从 tmux session 分离（tmux session 继续运行）
- 支持 -L <socket> 指定 tmux socket

### 3. CLI -L 参数兼容
- muxterm -L <socket> <command> 要能工作（全局 -L 在子命令前）
- 可能需要调整 main.rs 的参数解析逻辑

### 4. 集成测试
- 新增 tests/tmux_backend_integration.rs
- 测试：创建 tmux session → muxterm 连接 → 验证 pane/window/layout
- 测试：attach → detach → re-attach 布局保持
- 测试：通过 muxterm 修改 → 原生 tmux 验证

## 约束
- 先出计划，再执行（plan-first）
- cargo test --no-default-features --features tui 必须全过
- cargo fmt
- commit 信息 feat(tmux): / fix(tmux): / test:
- 不要加 Co-authored-by
- 参考 tmux -CC 的真实输出测试，可以用 tmux -L <socket> -CC new-session 抓样例

## ⚠️ 关键：层级定义映射（必读）

muxterm 和 tmux 的层级定义不同，不能混用！

### muxterm 定义（TerminalModel 层，对外暴露的）
- Session → Window → Tab → Pane（4 层）
- Session: 一个终端会话（可后台）
- Window: 窗口（类似浏览器窗口）
- Tab: Window 内的标签页（类似浏览器 tab）
- Pane: Tab 内的分割区域（终端实际运行的地方）

### tmux 定义（只在 TmuxBackend 内部，不泄漏到 TerminalModel 层）
- session → window(=tab) → pane（3 层）
- tmux 的 window 就是 muxterm 的 tab
- tmux 没有 muxterm 的 Window 概念

### TmuxBackend 内部的映射规则
- tmux session → muxterm Session
- tmux window → muxterm Tab（不是 Window！）
- tmux pane → muxterm Pane
- muxterm Window → TmuxBackend 内部虚拟概念，一个 tmux session 对应一个 muxterm Window

**在 TmuxBackend 实现中，必须做这个转换。TerminalModel 层永远只看到 Session→Window→Tab→Pane。tmux 的 3 层定义不能泄漏。**
