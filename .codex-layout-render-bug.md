# Bug 修复 + CLI session 架构

## 概述

两个问题需要修复：
1. CLI 每次命令独立 session，状态不保留 → 需要像 tmux 一样的 session 架构
2. TUI 渲染不递归布局树 → 嵌套分割显示错误

---

## 问题 1: CLI session 架构（参考 tmux）

### 当前问题

`cli_mode()` 每次创建新 LocalBackend → connect → 执行 → shutdown。命令间状态不保留。

### 目标设计（参考 tmux 的 server/client 架构）

CLI 应该像 tmux 一样：

```
# 创建一个持久 session（启动 daemon 进程）
muxterm new-session -s mywork

# 后续命令连接到同一 session
muxterm -s mywork split-pane -h
muxterm -s mywork split-pane -v -t @2
muxterm -s mywork list-panes
muxterm -s mywork list-layout
muxterm -s mywork send-keys -t @1 "echo hello"
muxterm -s mywork capture-pane -t @1
muxterm -s mywork kill-session
```

### 实现方案

**daemon 进程 + unix socket IPC：**

1. `muxterm new-session -s <name>` 
   - fork 后台 daemon 进程
   - daemon 持有 LocalBackend + TerminalModel（保持 connect 状态）
   - daemon 监听 unix socket: `/tmp/muxterm-<name>.sock`
   - daemon 接收 CLI 命令 → 执行 Task → 返回格式化输出

2. `muxterm -s <name> <command>`
   - 连接到 `/tmp/muxterm-<name>.sock`
   - 发送 CliCommand（序列化）
   - 接收格式化输出并打印

3. `muxterm list-sessions`
   - 扫描 `/tmp/muxterm-*.sock`
   - 列出所有活跃 session

4. `muxterm kill-session -s <name>`
   - 发送 shutdown 信号到 daemon
   - daemon 关闭 backend + 删除 socket

### 注意事项
- 参考 tmux 的 `-L <socket-name>` 和 `-s <session-name>` 设计
- session name 是用户友好的名字，socket 路径用 session name 派生
- daemon 需要处理 SIGTERM/SIGINT 优雅退出
- 现有的单命令模式（不指定 -s）保持兼容：直接创建临时 session 执行后关闭

### 代码结构建议
- `src/cli/daemon.rs` — daemon 进程逻辑（监听 socket、执行命令）
- `src/cli/client.rs` — client 端（连接 socket、发送命令、接收输出）
- `src/cli/session.rs` — session 管理（创建/查找/销毁 daemon）
- `src/main.rs` — 根据 -s 参数决定走 daemon 模式还是单命令模式

---

## 问题 2: TUI 渲染不递归布局树

### 当前问题

`render_frame()` 用 `tree.leaves()` 拍平所有 pane，均分列宽并排渲染。不递归 `LayoutNode`。

**复现：**
1. Alt+S 水平分割 → 左右 `@1 | @2` ✅
2. Alt+V 对右侧 @2 垂直分割 → **应该** 左 @1 | 右上 @2 / 右下 @3
   **实际** 三列并排 `@1 | @2 | @3` ❌

### 修复要求

`render_frame()` 递归 `LayoutNode`：
- `Leaf(pid)` → 在分配的矩形区域内渲染 pane 输出
- `Split { Horizontal, first, second, ratio }` → 左右分割，first 占 ratio/1000 列宽
- `Split { Vertical, first, second, ratio }` → 上下分割，first 占 ratio/1000 行数

pane 标题栏也要递归渲染。

---

## 测试要求

### 集成测试（`tests/cli_integration.rs`）

用已有的 `make_model()` + `exec_and_drain()` 模式测试嵌套分割：

```rust
#[test]
fn cli_nested_split_layout_tree() {
    let mut model = make_model();
    let pane1 = model.state().active_pane().unwrap().id;
    
    // 水平分割 → @1 | @2
    exec_and_drain(&mut model, Task::SplitPane {
        target: Some(pane1), dir: SplitDir::Horizontal, ..
    });
    
    // 切到 @2
    let pane2 = model.state().active_pane().unwrap().id;
    assert_eq!(pane2, PaneId(2));
    
    // 对 @2 垂直分割 → @2 | @3 (上下)
    exec_and_drain(&mut model, Task::SplitPane {
        target: Some(pane2), dir: SplitDir::Vertical, ..
    });
    
    // 验证布局树: Split(H, @1, Split(V, @2, @3))
    let out = format_output(model.state(), &CliCommand::ListLayout { .. }, OutputFormat::Json);
    assert!(out.contains("\"dir\":\"horizontal\""));
    assert!(out.contains("\"dir\":\"vertical\""));
    
    // 验证 3 个 pane
    let panes = format_output(model.state(), &CliCommand::ListPanes { .. }, OutputFormat::Json);
    assert!(panes.contains("@1") && panes.contains("@2") && panes.contains("@3"));
}
```

### 渲染单元测试（`src/platform/tui/render.rs`）

用 MockBackend 构造 `Split(H, @1, Split(V, @2, @3))`，验证：
- @1 内容在左半区域
- @2 内容在右上
- @3 内容在右下

### CLI daemon 测试
- 创建 session → split → list → kill 全流程
- 多个 session 并存
- session 不存在时的错误处理

---

## 优先级
1. **先修渲染 bug**（问题 2）+ 加集成测试 — 较小，可以快速验证
2. **再做 CLI session 架构**（问题 1）— 较大，需要 daemon + IPC

## 约束
- `cargo test --no-default-features --features tui` 必须全过
- `cargo fmt` 格式化
- commit 信息 `fix(tui):` / `feat(cli):` / `test:`
- 不要加 Co-authored-by
- 参考 tmux 的 CLI 设计但不要照搬 tmux 的实现