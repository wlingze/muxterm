# dogfood 2026-08-15 15:40（Phase C 之后，Phase D 必读）
# 源：仓库根目录 test_2026-0815-1540.log（约 178KB / 48s，gitignored）
# 时间：2026-08-15T07:41:05Z — 07:41:53Z
# 场景：Linux GTK SSH attach
#   spawn: ssh -tt ryzen -- tmux -CC attach -t legion
# Codex 动手前读完本文，再按 LINUX-PLAN §2 对原日志 rg。不要 include_str 原日志。
#
# ========== 已核实（不要再争论）==========
#
# 已好（不要当 C8 去重做 session-id）：
#   send: list-windows -t $1
#   execute task SwitchTab { target: TabId(10) } 以及 5/7/24/25 多次
#   全文没有「忽略其它 session」
#
# 仍坏（LINUX-PLAN §4）：
# D1 画面：attach 立刻 capture-pane（%15/%57/%25/%18/%26/%53/%56）且大量
#     「实时 %output 交付」pane=25/53，len 常 1365/2730。数据在 backend。
#     VTE/Codex 空或碎 = visible_ansi 有损 + 空网格 seeded。
# D2 滚动：镜像 VTE scrollback=0，日志里没有滚动/copy-mode；UI 没读 replica 历史。
# D3 状态点：全程无 popover/clicked 相关日志。点被 GTK Button 吃掉；无颜色 CSS。
#
# WARN：仅 detach 附近
#   send: detach-client -t $1
#   WARN tmux 命令 … 出错（丢弃查询 Ignore）
# 不要当 parser 失败去修。
#
# ========== 从日志抽出的核对句（给 rg，不是给 parse_line）==========
# 本日志是 tracing 行，不是 tmux % 控制流。C8 用合成网格，不要把下面 include_str 进 parser。

list-windows -t $1
capture-pane -e -p -t %25
capture-pane -e -p -t %53
SwitchTab { target: TabId(10) }
detach-client -t $1
