# TmuxBackend 修复 Round 6: CLI attach-session 命令解析 + list-windows 返回正确 session

## 必须通过的端到端测试

以下测试必须用真实二进制（../muxterm-target/debug/muxterm）执行通过，不能只测 API 层。

### 预备：创建 tmux session
```
tmux -L test1 new-session -d -s demo -x 80 -y 24
tmux -L test1 split-window -h -t demo
tmux -L test1 select-pane -t demo:0.1
tmux -L test1 split-window -v -t demo:0.1
tmux -L test1 new-window -t demo
```

### CLI 测试（必须全过）
```
# 1. list-sessions 列出 demo
muxterm list-sessions -L test1
# 期望: 包含 "demo" session

# 2. attach-session -t demo 连接已有 session
muxterm attach-session -t demo -L test1
# 期望: 不报错

# 3. attach 后 list-windows 显示 demo 的 2 windows
muxterm list-windows -L test1
# 期望: 2 windows（不是新创建的 1 window）

# 4. attach 后 list-panes 显示 active tab 的 panes
muxterm list-panes -L test1
# 期望: tab1 有 3 panes 或 tab2 有 1 pane

# 5. attach 后 list-layout 显示嵌套布局
muxterm list-layout -L test1 --format text
# 期望: 嵌套分割树

# 6. select-window 切 tab
muxterm select-window -t w1 -L test1
# 期望: 切到 tab1

# 7. split-pane 操作已有 session
muxterm split-pane -h -L test1
# 期望: tmux 验证 pane 增加

# 8. send-keys
muxterm send-keys -t @0 "echo hello" -L test1
# 期望: capture-pane 看到 hello
```

### TUI 测试（必须全过）
```
# 9. TUI attach 到已有 session
muxterm --tui -L test1 attach -t demo
# 期望: 显示 2 tabs，tab1 有 3 panes 嵌套布局

# 10. Alt+1 切到 tab1
# 期望: 显示 3 panes（左1右上下2）

# 11. Alt+2 切到 tab2
# 期望: 显示 1 pane

# 12. echo hello + Enter
# 期望: 输出显示在 TUI

# 13. Alt+S split
# 期望: pane 增加

# 14. Ctrl-Q 退出
# 期望: tmux session 继续存在（detach 语义）
```

## 当前 Bug

### Bug 8: attach-session -t demo 报错 "缺少参数: -t session"
- parse_cli_command 不认 attach-session 的 -t 参数
- 需要检查 src/cli/command.rs 的 AttachSession 解析逻辑

### Bug 9: list-windows -L 返回新创建的 session 数据，不是已有 session
- cli_mode_tmux 用 TmuxBackend::new（new-session 模式）而不是 attach
- 需要默认 attach 到第一个已有 session（如果有的话）
- 或者 list-windows 在 -L 模式下自动 attach

### Bug 10: TUI -L 只能 new-session，不能 attach
- TUI 没有处理 attach 参数
- 需要 muxterm --tui -L <socket> attach -t <name> 支持

## 修复策略

1. 修 parse_cli_command 支持 attach-session -t <name> -L <socket>
2. CLI -L 模式下默认 attach 到第一个已有 session（不是创建新的）
3. TUI 支持 attach 参数
4. 每个修复都加端到端测试（用真实二进制执行）
5. 不要只测 API 层，必须测 CLI 命令行解析 + 执行

## 关键原则
- 测试必须用真实二进制 `../muxterm-target/debug/muxterm` 执行
- 不能只测 `TmuxBackend::new_with_attach()` API 调用
- 必须测 `muxterm attach-session -t demo -L test1` 完整命令行
- 修复后必须编译 + 运行真实二进制 + 验证输出

## 约束
- cargo test --no-default-features --features tui 必须全过
- cargo fmt + clippy -- -D warnings
- commit: fix(cli): / fix(tmux): / test(tmux):
- 不要 Co-authored-by