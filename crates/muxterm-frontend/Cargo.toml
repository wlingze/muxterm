[package]
name = "muxterm-frontend"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
anyhow = "1"
muxterm-core = { path = "../muxterm-core", features = ["ffi"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tracing = "0.1"
