[package]
name = "muxterm-protocol"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
schemars = "1.2"
serde = { version = "1", features = ["derive"] }
thiserror = "1"

[dev-dependencies]
serde_json = "1"
