# Muxterm CLI 命令集 — 完整设计 Spec

## 层级模型

```
Session (对话，可后台运行无窗口)
  ├─ Window (窗口，一定绑定一个 session)
  │    └─ Tab (标签页)
  │         └─ Pane (窗格)
  └─ (无 Window，后台运行)
```

- Session 是逻辑对话，Window 是 UI 窗口
- Session 可以没有 Window（后台跑着但不显示）
- Window 一定属于某个 Session
- 这不是 tmux 的定义。tmux 的定义（session→window=tab→pane）只在 TmuxBackend 内部存在，TerminalModel 层用 muxterm 自己的定义

## 命令列表

### Session 管理

| 命令 | 缩写 | 参数 | 说明 |
|------|------|------|------|
| `new-session` | `new` | `[-n name] [-s socket]` | 创建新 session + 默认 window |
| `kill-session` | | `[-t session]` | 关闭 session（含所有 window/tab/pane） |
| `list-sessions` | `ls` | `[-F format]` | 列出所有 session（含后台） |
| `attach-session` | `attach` | `-t session` | 把后台 session 接到 window 显示 |
| `detach` | | `[-t session]` | session 脱离 window，变后台 |
| `rename-session` | | `new-name` | 重命名 session |

### Window 管理

| 命令 | 缩写 | 参数 | 说明 |
|------|------|------|------|
| `new-window` | `neww` | `[-n name] [-t session]` | 在 session 内新建 window |
| `kill-window` | `killw` | `[-t window]` | 关闭 window |
| `list-windows` | `lsw` | `[-t session]` | 列出 session 的所有 window |
| `select-window` | `selectw` | `-t window` | 切换到 window |
| `rename-window` | `renamew` | `new-name` | 重命名 window |

### Tab 管理

| 命令 | 缩写 | 参数 | 说明 |
|------|------|------|------|
| `new-tab` | | `[-n name] [-t window]` | 在 window 内新建 tab |
| `kill-tab` | | `[-t tab]` | 关闭 tab |
| `list-tabs` | `lst` | `[-t window]` | 列出 window 的所有 tab |
| `select-tab` | | `-t tab` | 切换到 tab |
| `rename-tab` | | `new-name` | 重命名 tab |

### Pane 管理

| 命令 | 缩写 | 参数 | 说明 |
|------|------|------|------|
| `split-pane` | `splitp` | `[-h\|-v] [-l size] [-t pane]` | 分割 pane（水平/垂直） |
| `kill-pane` | `killp` | `[-t pane]` | 关闭 pane |
| `list-panes` | `lsp` | `[-t tab]` | 列出 tab 的所有 pane |
| `select-pane` | `selectp` | `-t pane` | 切换到 pane |
| `resize-pane` | `resizep` | `-t pane -x w -y h` | 调整 pane 大小 |

### 输入输出

| 命令 | 缩写 | 参数 | 说明 |
|------|------|------|------|
| `send-keys` | `send` | `-t pane "text"` | 发送按键到 pane |
| `capture-pane` | `capturep` | `-t pane [-S lines]` | 抓取 pane 输出内容 |

### 布局查询

| 命令 | 参数 | 说明 |
|------|------|------|
| `list-layout` | `[-t window]` | 输出布局树（嵌套结构 + 尺寸） |
| `display-message` | `-t pane -F format` | 格式化输出 pane/session 信息 |

## 输出格式

默认 JSON，`--format text` 可读文本。

### list-sessions (JSON)
```json
[{"id":"$1","name":"dev","windows":2,"attached":true},{"id":"$2","name":"bg","windows":0,"attached":false}]
```

### list-sessions (text)
```
$1: dev (2 windows, attached)
$2: bg  (0 windows, detached)
```

### list-windows (JSON)
```json
[{"id":"w1","name":"main","tabs":2,"session":"$1"}]
```

### list-tabs (JSON)
```json
[{"id":"t1","name":"shell","panes":2,"active":true},{"id":"t2","name":"logs","panes":1,"active":false}]
```

### list-panes (JSON)
```json
[{"id":"@1","active":true,"size":{"w":80,"h":24}},{"id":"@2","active":false,"size":{"w":40,"h":24}}]
```

### list-layout (text)
```
window w1: main
├─ tab t1: shell [active]
│  └─ horizontal split
│     ├─ @1 [active] 40x24
│     └─ @2         40x24
└─ tab t2: logs
   └─ @3 80x24
```

### capture-pane
```
$ echo hello
hello
$ _
```

## 使用方式

```bash
# 交互式 TUI（人用）
muxterm --tui

# 单命令模式（agent/测试用）
muxterm new-session -n dev
muxterm split-pane -h -t @1
muxterm list-panes
muxterm send-keys -t @1 "echo hello"
muxterm capture-pane -t @1
muxterm kill-session

# 一串命令
muxterm new-session -n test && muxterm split-pane -h && muxterm list-panes
```

## 实现方式

CLI 命令复用现有 TerminalModel + Backend 接口：
```
muxterm <command> <args>
  ↓
  parse command → TerminalTask
  ↓
  TerminalModel::execute(task)
  ↓
  Backend (Local/Tmux) 执行
  ↓
  State 查询 → 格式化输出 (JSON/text)
```

不经过任何 UI 渲染层，直接调 Core 层 API。

## TerminalModel 层级模型更新

当前 model 里的层级是 session → window → pane（参考 tmux）。需要改为：
```
Session
  └─ Window
       └─ Tab
            └─ Pane
                 └─ LayoutNode (分割树)
```

新增 Tab 层级。LayoutTree 的根从 Window 改为 Tab。

## Backend 映射

### LocalBackend
- Session = 一次连接（可后台，进程不杀）
- Window = 自管理窗口
- Tab = 自管理标签页
- Pane = pty 子进程
- detach = window 关闭但 session 进程继续跑

### TmuxBackend
- Session = tmux session
- Window = 自管理（可对应一个 tmux session 的视图）
- Tab = tmux window
- Pane = tmux pane
- attach/detach = tmux 的 attach/detach

tmux 的层级映射在 TmuxBackend 内部完成，TerminalModel 层不感知 tmux 的定义。

## 测试策略

### 单元测试
- 命令解析：`parse_command("split-pane -h -t @1")` → `TerminalTask::SplitPane{...}`
- 格式化输出：构造 MockBackend state → 断言 JSON/text 输出正确

### 集成测试（CLI 端到端）
```bash
muxterm new-session -n test
muxterm split-pane -h
muxterm list-panes --format json | jq '.panes | length'  # 应输出 2
muxterm send-keys -t @1 "echo hi"
muxterm capture-pane -t @1 | grep "hi"  # 应匹配
muxterm kill-session
```

### Agent 友好
Codex/Cursor 可以直接用 CLI 命令验证：
- `muxterm list-layout` 看结构
- `muxterm capture-pane` 看输出
- `muxterm send-keys` 操作 pane

## 实施计划

1. 更新 TerminalModel 层级：加 Tab 层（Session→Window→Tab→Pane）
2. 更新 State trait：加 tabs 查询
3. 更新 TerminalTask：加 NewTab/KillTab/SwitchTab/RenameTab
4. 更新 LocalBackend：实现 Tab 层级
5. 更新 TmuxBackend：Tab = tmux window 映射
6. 实现 CLI 命令解析器（clap subcommand）
7. 实现输出格式化（JSON + text）
8. 写单元测试 + 集成测试
9. cargo test 全量通过
10. commit + push

## 约束
- 不碰 GTK 前端（双轨，GTK 保持旧路径）
- 现有 355+ 测试必须保留并回归通过
- tmux 的定义只在 TmuxBackend 内部，不泄漏到 TerminalModel
- commit 信息: feat: / test: / refactor:
- 不要加 Co-authored-by