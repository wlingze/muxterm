# Muxterm 架构重构任务

## 重要:先出完整计划,我确认后再执行

不要急着写代码。先读这个文件,理解架构设计,然后输出一份完整的计划:
- 目录结构设计
- 每个 trait / struct / enum 的职责
- 文件清单(每个文件做什么)
- 测试策略(每个模块的测试)
- 实施顺序(先做哪层,再做哪层)

计划确认后,再开始写代码。

---

## 核心设计:两层抽象 + Terminal 纯模型 + 多种前端

### 架构总览

```
┌─────────────────────────────────────────────────┐
│  Platform 层 (前端)                              │
│  ┌────────────────┐ ┌──────────┐ ┌───────────┐  │
│  │ GTK4 (Linux)   │ │ Terminal │ │ ASCII TUI │  │
│  │ 原生桌面        │ │ (CLI)    │ │ (测试用)   │  │
│  └────────┬───────┘ └────┬─────┘ └─────┬─────┘  │
└───────────┼──────────────┼──────────────┼────────┘
            │ 渲染 State   │ 接收 Task    │
            ▼              ▼              ▼
┌─────────────────────────────────────────────────┐
│  Terminal 层 (纯模型,无 I/O,无 GUI 依赖)        │
│                                                  │
│  - SessionManager: 管理 session/window/pane 树   │
│  - LayoutTree: 分割布局(水平/垂直)               │
│  - 纯数据结构,所有操作是纯函数                    │
│  - 不依赖 GTK,不依赖 tmux,不依赖任何 I/O         │
│  - 测试: 纯函数单元测试,无需 DISPLAY              │
└───────────┬──────────────────────┬────────────────┘
            │ 使用 State trait     │ 实现 Task trait
            ▼                      ▼
┌─────────────────────────────────────────────────┐
│  Backend 层 (两种实现)                           │
│                                                  │
│  ┌─────────────────────┐  ┌───────────────────┐ │
│  │ TmuxBackend         │  │ LocalBackend       │ │
│  │                     │  │                    │ │
│  │ State ← tmux 控制协议│  │ State ← 自己维护   │ │
│  │   (%layout-change,  │  │   (本地 shell 的   │ │
│  │    %output,         │  │    tab/pane 布局)   │ │
│  │    %window-add...)  │  │                    │ │
│  │                     │  │                    │ │
│  │ Task → send-keys,   │  │ Task → spawn 进程, │ │
│  │   split-window,     │  │   vte4 操作,       │ │
│  │   kill-pane, ...    │  │   本地 shell 管理   │ │
│  └─────────────────────┘  └───────────────────┘ │
└─────────────────────────────────────────────────┘
```

### 两个核心 trait

```rust
/// 状态:当前 session/window/pane 的完整快照
trait State {
    fn sessions(&self) -> &[SessionInfo];
    fn active_session(&self) -> Option<&SessionInfo>;
    fn active_window(&self) -> Option<&WindowInfo>;
    fn active_pane(&self) -> Option<&PaneInfo>;
    fn layout(&self, window_id: &WindowId) -> Option<&LayoutTree>;
    // 流式输出: pane 的 stdout 字节流
    fn pane_output(&self, pane_id: &PaneId) -> Option<&[u8]>;
}

/// 任务:一个可执行的操作
trait Task {
    type Output;
    fn execute(&mut self) -> Result<Self::Output>;
}

/// 具体任务类型
enum TerminalTask {
    SplitWindow { direction: SplitDir, target: PaneId },
    ClosePane { target: PaneId },
    SendKeys { target: PaneId, keys: Vec<KeyEvent> },
    NewWindow,
    SwitchPane { target: PaneId },
    ResizePane { target: PaneId, width: u16, height: u16 },
    // ...
}
```

### Backend 层

两个实现,各自实现 State 和 Task:

**TmuxBackend:**
- State 来自 tmux 控制协议解析(`%layout-change`, `%output`, `%window-add`...)
- Task 映射为 `send-keys -t`, `split-window`, `kill-pane`, `resize-pane` 等命令
- 关键: tmux 内和我们的 terminal 看到的必须一致。创建 tab → 映射到 tmux 内创建 window/pane,分割窗口 → 映射到 tmux 的 split-window。状态变化从 tmux 控制协议同步回来。
- 现有 tmux 协议解析器(protocol.rs, client.rs, command.rs)集成到这里

**LocalBackend:**
- State 自己维护(本地 shell 的 tab/pane 布局树)
- Task 映射为 spawn 子进程、vte4 操作
- 现在已有的本地 shell 实现(没有 tmux 时)集成到这里

### Terminal 层(纯模型)

核心是 TerminalModel,使用 Backend 的 State 和 Task:

```rust
struct TerminalModel {
    backend: Box<dyn Backend>,  // TmuxBackend | LocalBackend
    state: Box<dyn State>,
    history: Vec<StateSnapshot>,  // 用于 undo/redo
}

impl TerminalModel {
    fn execute(&mut self, task: TerminalTask) -> Result<()>;
    fn current_state(&self) -> &dyn State;
    fn subscribe(&mut self, cb: Box<dyn Fn(StateChange)>) -> Subscription;
}
```

- 纯 Rust 数据结构,无 I/O,无 GUI 依赖
- 所有操作可测试(输入 task → 验证 state 变化)
- 所有测试可以 `cargo test` 跑,不需要 DISPLAY

### Platform 层(前端)

多种前端实现,都只做一件事:**渲染 State + 接收用户操作转成 Task**:

| 前端 | 技术 | 用途 |
|------|------|------|
| GTK4 (Linux) | gtk4-rs + vte4 | 桌面原生(已有) |
| Terminal (CLI) | crossterm / termion | 纯终端使用 |
| **ASCII TUI (测试)** | 简单文本渲染 | **测试界面用,抓取输出对比** |

### 测试策略

每一层都应有大量测试:

**Backend 层测试:**
- TmuxBackend: 保持现有 270 个测试(协议解析、命令构造、end-to-end tmux spawn)
- 新增: State trait 的测试(模拟 tmux 事件 → 验证 State 正确)
- 新增: Task 映射测试(执行 Task → 验证 tmux 收到正确命令)

**Terminal 层测试(核心):**
- 创建/关闭 session/window/pane
- 水平/垂直分割布局
- 在分割后切换 pane
- resize pane
- 状态同步(模拟 tmux 事件 → 验证 TerminalModel 状态正确)
- 所有测试不需要 GTK,不需要 DISPLAY

**Platform 层测试:**
- GTK4: 集成测试(需要 DISPLAY)
- ASCII TUI: 输出抓取对比测试

### 实施顺序

1. 先定义核心 trait(State, Task, Backend) — 纯接口,无实现
2. 实现 TerminalModel — 纯模型,依赖 trait 接口
3. 重构 LocalBackend — 从现有代码提取,实现 State + Task
4. 重构 TmuxBackend — 从现有代码提取,集成到新架构
5. 实现 ASCII TUI 前端(测试用)
6. 重构 GTK4 前端 — 适配新架构
7. 回归测试:全部 270+ 测试通过

### 约束

- 不要因为现有的代码束手束脚,这是大重构。什么都可以改。
- 但现有测试必须保留并回归通过(270 passed)
- 现有功能必须保留:本地 shell tab、tmux -CC attach、SSH 远程、命令面板、配置
- 工作树要保持干净,不要遗留未跟踪文件
- 每完成一层,用 worktree 或分支提交,不要直接改 main