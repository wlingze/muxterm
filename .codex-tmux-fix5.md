# TmuxBackend 修复 Round 4: CLI -L 用 TmuxBackend + TUI Alt+N window id 映射

## Bug 5: CLI -L 参数没传到 TmuxBackend

### 现象
- muxterm list-sessions -L muxterm-demo → 返回 LocalBackend 数据（local session），不是 tmux 的 demo session
- cli_mode_ephemeral() 永远创建 LocalBackend，忽略 -L 参数

### 根因
- main.rs 的 cli_mode() → cli_mode_ephemeral() 不检查 -L 参数
- -L 只在 TUI 模式（platform::tui::app::run(cli.socket)）才生效
- CLI 命令模式需要也检查 -L 并创建 TmuxBackend

### 修复要求
1. cli_mode() 或 cli_mode_ephemeral() 检查 args 中的 -L 参数（类似 extract_session_name 提取 -s）
2. 有 -L 时创建 TmuxBackend（config 指定 socket name），而不是 LocalBackend
3. TmuxBackend connect 后执行命令，格式化输出，然后 shutdown
4. 测试：muxterm list-sessions -L <socket> 返回 tmux session 数据
5. 测试：muxterm list-windows -L <socket> 返回正确的 window 数
6. 测试：muxterm list-panes -L <socket> 返回正确的 pane 数
7. 测试：muxterm split-pane -h -L <socket> 通过 TmuxBackend 操作 tmux

## Bug 6: TUI Alt+数字切 tab window id 映射不匹配

### 现象
- TUI 连接 2 tab tmux session，初始在 tab2（1 pane）正常显示
- Alt+1 切到 tab1 → 显示 0 panes (no pane)
- Alt+2 切回 tab2 → 也变成 0 panes

### 根因
- TUI key_event_to_task: Alt+1 → Task::SwitchWindow { target: WindowId(1) }
- LocalBackend 的 WindowId 从 1 开始（1, 2, 3...）
- TmuxBackend 的 window 从 0 开始（tmux @0, @1）
- TmuxBackend 映射 window 时可能用了 tmux 的原始 index，导致 WindowId(1) 对应 tmux @1（tab2），WindowId(0) 不存在或不被发送
- Alt+1 发 WindowId(1) → 切到 tmux window 1（tab2），Alt+2 发 WindowId(2) → tmux window 2 不存在

### 修复要求
1. 确认 TmuxBackend 的 WindowId/TabId 编号规则
2. 确保 Alt+N 映射到正确的 tmux window：
   - 方案 A: TmuxBackend 的 WindowId 从 1 开始（tmux @0 → WindowId(1), @1 → WindowId(2)）
   - 方案 B: TUI 的 Alt+N 映射改成 WindowId(N-1)
   - 推荐方案 A，和 LocalBackend 保持一致
3. 切 tab 后 pane 数据正确显示（之前的修复已确保 connect 时获取所有 tab 的 pane）
4. 测试：2 tab session，Alt+1 切 tab1 显示 3 panes，Alt+2 切 tab2 显示 1 pane
5. 测试：切回 tab 后 pane 数据不丢失

## 测试要求

### CLI 测试（tests/tmux_backend_integration.rs 或新文件）
- muxterm list-sessions -L <socket> → 返回 tmux session
- muxterm list-windows -L <socket> → 返回正确 window 数
- muxterm list-panes -L <socket> → 返回正确 pane 数
- muxterm list-layout -L <socket> → 返回嵌套布局
- muxterm split-pane -h -L <socket> → tmux 验证 pane 增加
- muxterm new-window -L <socket> → tmux 验证 window 增加
- muxterm select-window -L <socket> → 切 tab 后 list-panes 变化
- muxterm send-keys -L <socket> → capture-pane 验证输出

### TUI 测试
- 2 tab 3 pane session：Alt+1 切 tab1 显示 3 panes
- Alt+2 切 tab2 显示 1 pane
- 切回后 pane 不丢失
- pty 输出正常显示

### 边界
- 单 tab session Alt+1
- Alt+9 不存在 tab
- CLI -L 不存在的 socket
- CLI 无 -L 仍用 LocalBackend（兼容）

## 约束
- cargo test --no-default-features --features tui 必须全过
- cargo fmt + clippy -- -D warnings
- commit: fix(tmux): / fix(cli):
- 不要 Co-authored-by
