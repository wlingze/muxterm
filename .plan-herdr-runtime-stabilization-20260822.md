# Herdr Runtime 稳定化实施计划（2026-08-22）

> 状态：W10 本地实现与门禁已完成；2026-08-25 修复旧 CI 基线暴露的 rustfmt 缺口；远端 CI 尚未在本轮 push 后复核
> 目标分支：`feature/runtime/support_herdr`
> 计划基线：`4e81f77`
> 最终复核：`2026-08-25T12:10:25+08:00`（CST；本机版本、旧 PR #20 required logs、
> 当前工作树与本地门禁均已只读核对；当前 HEAD 尚未产生新的远端 run）
> Runtime 专项设计：[`docs/HERDR-RUNTIME-STABILITY.md`](docs/HERDR-RUNTIME-STABILITY.md)
> 专项测试契约：[`docs/HERDR-TESTING.md`](docs/HERDR-TESTING.md)

本文是交给后续实现 agent 的施工单，不是问题讨论稿。执行者必须按本文的依赖顺序、
RED→GREEN 边界和完成门禁推进；不得只修某个截图、放宽现有测试或用一次手工成功替代
矩阵证据。

---

## 0. 结论先行

本轮不是四个互不相关的 GTK bug，而是三条共享故障链：

1. **Herdr stream 所有权错误**：当前每个 pane 都启动
   `ControlTerminal { takeover: true }`，以 `Vec<ObserveStream>` 保存，事件没有
   generation，且丢弃 Herdr frame wire seq。旧流的 Error/Closed 可以删除新流并立即
   重建，造成 takeover 风暴、CPU 100% 和 pane 卡死。
2. **Index 与 Surface 数据源混用**：`pane.read visible_ansi` 被作为 `PaneFrame` 送进
   VTE；切 tab 时又按当前布局重新 seed 100–393KB 的 ANSI dump。结构、尺寸和
   `PaneFrame` 的批处理顺序不完整，因此出现 3/4 高度、横跨全屏乱码和 Ctrl-L 后旧屏复活。
3. **创建与连接身份不是权威收敛**：`tab.create`/`pane.split` 响应被立即写成本地最终
   拓扑，新 pane 初始化入口不统一；Project 又把 `path` 同时当目录与 Herdr
   `workspace_id`，失败时仍用 path-as-id 继续 attach。由此产生空 tab 名、创建后无流、
   split 卡死和“Project 失败但 Existing Connection 成功”。

最终修复必须同时满足：

- pane-keyed、带 generation/event ordinal、control intent/suppression 且保留 Herdr wire seq
  的单一 stream registry；
- 前台 active pane 才持有 control，其余 pane 只 observe；
- `pane.read` 只进 Index，`terminal.frame` 原始字节才进 Surface；
- tab/pane mutation 由 Herdr snapshot/layout/focus 权威收敛；
- Project 与 Existing Connection 解析为相同 `WorkspaceSpec`/`WorkspaceId`；
- 真实 `+`、Alt+T、Alt+S/V、Ctrl-L 和 AppWindow/VTE 进入 required 自动化；
- 本地与 CI 使用相同工具版本、fixture、脚本入口和 skip 规则。

任何只完成其中一部分的结果，都只能报告“部分修复”，不能报告 Herdr runtime 完成。

---

## 1. 文档优先级与实现边界

### 1.1 权威文档

执行时按以下优先级理解契约：

1. [`docs/WORKSPACE.md`](docs/WORKSPACE.md)：产品树和 Core/platform 边界；
2. [`docs/RUNTIME.md`](docs/RUNTIME.md)：所有 Runtime 的公共能力与字节契约；
3. [`docs/HERDR-RUNTIME-STABILITY.md`](docs/HERDR-RUNTIME-STABILITY.md)：本轮 Herdr
   lifecycle、mutation、identity 的专项定案；
4. [`docs/SURFACE.md`](docs/SURFACE.md)：像素路径，One Surface / No dump；
5. [`docs/CATALOG.md`](docs/CATALOG.md)：Driver/Transport/Connect/Pool 与打开路径；
6. [`docs/HERDR-TESTING.md`](docs/HERDR-TESTING.md)：本轮测试层次、阈值和 CI 契约；
7. [`docs/TESTING.md`](docs/TESTING.md)：全项目通用测试纪律。

若实现中发现这些文档互相冲突，先停止编码、列出具体冲突并修正文档；禁止实现者自行
选择最省事的一份。

### 1.2 本轮允许修改

- `src/core/model/`：公共 Runtime/StateChange 契约；
- `src/core/workspace/`：foreground 驱动、Index、WorkspaceSpec 身份；
- `src/core/runtime/herdr/`：wire、stream registry、mutation convergence；
- `src/core/catalog/`、`src/core/quickconnect/`：统一 resolver 与持久化身份；
- `src/platform/linux/`：只做通用事件批处理和真实生产入口接线；
- `tests/`、`scripts/test.sh`、`scripts/ci/`、`.github/workflows/`、工具链声明；
- 与接口变更直接相关的 FFI/macOS 适配和回归测试。

### 1.3 明确不做

- 不修改 `~/Developer/terminal/{herdr,herdrm}`；当前 Herdr checkout 已是 protocol 20，
  只能只读参考概念，不能把它的 wire enum/字段顺序复制进 protocol-19 分支；
- 不加入 GUI `if runtime == "herdr"`；
- 不在 platform 新增连接池、Herdr socket client 或身份 side table；
- 不以 `visible_ansi`、Replica dump、周期 reset 或更长 sleep 修像素问题；
- 不为远程用户安装/启动 Herdr；
- 不访问或破坏默认 tmux server、默认 Herdr session；
- 不把 loopback SSH 结果外推为任意真实 Host 已通过；
- 不顺手重构无关 TUI、主题、attention、worktree UI。

### 1.4 工作树保护

计划编写时 `scripts/test.sh` 已有用户的 8 行格式化改动。实现 agent 开始时必须：

```bash
rtk date -Iseconds
rtk date
rtk pwd
rtk git status --short --branch
rtk git diff -- scripts/test.sh
```

若该改动仍在，先保存 diff 证据，后续 W8 修改同一文件时在它之上增量编辑，不得覆盖或
回退。每次只 `git add <本工作单文件>`，禁止 `git add -A`。

---

## 2. 当前证据与修改映射

### 2.1 四份用户日志

| 日志 | 可复现现象 | 当前代码证据 | 对应工作单 |
|---|---|---|---|
| `test_2026-0822-1517.log` | tab1 正常；tab2 固定在约 3/4；其它 tab 横向乱码；Ctrl-L 后残留 | `seed_raw` 重放 109–393KB；`seed_one_pane()` 把 `pane.read` 发成 `PaneFrame`；`dispatch_event_batch()` 没把 `PaneFrame` 归入延迟 Surface 阶段 | W1、W3、W4、W7 |
| `test_2026-0822-1530.log` | 点击 `+` 创建 tab 后卡住 | 按钮 handler 直接 `execute(NewTab)` 后同步 `refresh_ui`；`apply_created_tab()` 乐观建结构但不走统一 stream bootstrap | W5、W7 |
| `test_2026-0822-1531.log` | Alt+T 创建后乱码；新 tab 空名 | `name=None` 被写成 `"label": ""`；响应 label 可覆盖成空值 | W5、W7 |
| `test_2026-0822-1533.log` | split 服务端成功、Muxterm 卡死、CPU 100% | pane `@40` 反复出现 `terminal attach taken over` 与帧读取失败；旧/新流都只带 pane id，Error/Closed 立即重建 | W2、W5、W7 |

日志中还出现多个 pane 同时 `读 Herdr 帧长度失败`，说明问题不是单个 pane 内容损坏，
而是共享 lifecycle 失稳。

证据分级：表中当前函数行为、wire payload、空 label、takeover/error 循环是源码/日志直接
证据；这些机制与 3/4 高度、乱码、旧屏复活之间的逐现象因果闭环，要由 W2–W7 的最小 RED
与 GREEN 结果证明。执行者不得把本计划的因果解释写成“测试已经证明”，也不得因一次手工
正常跳过 RED。

### 2.2 当前实现中的确定性缺口

| 位置 | 当前行为 | 必须变为 |
|---|---|---|
| `src/core/runtime/herdr/observe.rs` | 名字叫 observe，实际总发 `ControlTerminal { takeover: true }` | mode-aware stream：后台 `ObserveTerminal`，前台 active pane 才 `ControlTerminal` |
| `src/core/runtime/herdr/runtime.rs` | `Vec<ObserveStream>` 可重复；event 无 generation；frame.seq 被丢弃；Error/Closed 立即重建 | `HashMap<PaneId, PaneStreamSlot>`；stale/gap event 收敛；有界 backoff |
| 同上 `seed_one_pane()` | `pane.read` → `PaneFrame` → Surface | `pane.read` → `PaneIndexSnapshot`，platform 永不 feed |
| 同上 `apply_created_tab/apply_split_pane` | 响应即最终拓扑；新 pane 初始化逻辑重复 | `PendingMutation` + snapshot 权威完成 + `ensure_pane_initialized()` |
| 同上 `Task::NewTab` | None label 序列化为空字符串 | None 时完全省略 `label`；空 label 用权威数字 fallback |
| `src/platform/linux/window.rs` | `PaneFrame` 在结构 batch 中可能提前 dispatch；创建按钮主动 refresh | 结构→一次 refresh→full frame→diff；创建只等事件收敛 |
| `src/core/protocol/ffi/api.rs` | `muxterm_poll_events` 把 background 批次去掉 WorkspaceId 后拍平；不同 Workspace 的同号 Pane 无法路由 | additive workspace event wrapper；旧 poll active-only；`CStateChange` ABI 不变 |
| `src/core/workspace/pool.rs` | active/background 不通知 Runtime；释放按 runtime 字符串判断 | `set_foreground()`；释放按 `PersistDetach` capability |
| `src/platform/linux/window.rs::connect_herdr*` | platform 直连 socket；错误后用 `config.path` 当 id | Catalog resolver 统一 Project/Existing；阶段错误直接失败 |
| `tests/linux_runtime_transport_matrix_e2e.rs` | 一个 AppWindow 跑全部 cell；大量 `test_handle_action/test_poll_once` | 每 cell/scenario 独立进程；真实 widget/key controller/GLib |
| CI | 三个 required 的 `@stable` 实际为 Rust 1.98.0，本机 1.97.1；CI tmux 3.7b、本机 3.7c；Linux 双层 Xvfb；macOS 另写命令且 SSH skip | 固定版本、单脚本入口、单层 Xvfb、required loopback SSH |

### 2.3 2026-08-22 CI 基线

最终状态复核时间：`2026-08-22T19:31:35+08:00`。PR
[#20](https://github.com/wlingze/muxterm/pull/20) 的三个 required run 均失败：

- [Core 32559662382](https://github.com/wlingze/muxterm/actions/runs/32559662382)：
  `herdr_authority_contract` 的 SSH right split 后 `Workspace layout.active` 未指向 created pane；
- [Linux 32559662385](https://github.com/wlingze/muxterm/actions/runs/32559662385)：
  `linux_herdr_agent_e2e` detach/reattach 后 `pane.read(w1:p2)` 缺 token，随后同一 test
  process SIGSEGV；
- [macOS 32559662392](https://github.com/wlingze/muxterm/actions/runs/32559662392)：
  `PaletteSessionListE2ETests.testLocalSessionListRefreshesBeyondNewSession` 只看到 `New`；
  两个 loopback SSH 用例被 skip。

四个旧 four-mode shell/tmux local/SSH job 绿色，只能证明那些 CLI case，不能替代 Herdr、
GTK、创建、Surface 或 Project identity。

---

## 3. 接口变更清单（先锁契约，再实现）

接口语义以 [`docs/HERDR-RUNTIME-STABILITY.md`](docs/HERDR-RUNTIME-STABILITY.md) §3–§8
为准。实现名称可以微调，以下能力不得省略。

### 3.1 Runtime 与 Pool

```rust
trait Runtime {
    fn set_foreground(&mut self, foreground: bool) { }
}
```

- `WorkspacePool::open/open_with_scrollback/insert_connected/activate` 必须把新 active runtime
  设为 `true`，把旧 active 设为 `false`；
- background poll 不改变 foreground；
- close/evict/shutdown 先设 false，再按 capability detach/shutdown；
- tmux/shell 默认 no-op，不允许 GUI 按 runtime 名判断。

异步 tab/pane mutation 还要把“已接收”和“已完成”分开：

```rust
TaskOutcome::Accepted { operation_id: u64 }
StateChange::MutationSettled {
    operation_id: u64,
    kind: MutationKind,       // NewTab | SplitPane
    result: MutationResult,   // Completed | Failed { stage, reason }
}
```

- `Done` 继续只表示同步操作已经完成；Herdr mutation 成功入有界队列返回 `Accepted`；
- `MutationSettled` 是唯一最终事件，失败阶段为
  `Queue/Dispatch/AuthorityConvergence/StreamBootstrap`，同一 operation 恰好一次；
- 现有 `muxterm_execute` 可把 `Accepted` 映射为返回码 0；新增 additive
  `muxterm_execute_json` 返回 Done/Accepted(operation_id)/Rejected 详情。最终 settlement 用
  `STATE_MUTATION_SETTLED=16`，把完整 JSON 放进既有 `CStateChange.data/data_len`，不改变
  `#[repr(C)] CStateChange` 布局；GTK 不得因 Accepted 手工 `refresh_ui()` 或显示“创建完成”。

### 3.2 Index 与 Surface

```rust
StateChange::PaneIndexSnapshot { pane: PaneId, data: Vec<u8> }
```

- Workspace 用它 `replace_snapshot`；
- Linux/macOS/TUI pixel dispatch 明确忽略它；
- `PaneFrame` 只表示 stream 当前 generation 的原始 full ANSI frame；
- `PaneOutput` 只表示该 generation 后续 diff；
- 不允许把新事件别名成现有 `PaneSnapshot`，因为后者在 tmux 重连路径仍有 Surface
  snapshot 语义。
- background/active 的 Surface event 在跨 FFI 时必须保留 WorkspaceId：新增
  `CWorkspaceStateChange { workspace_id, event: CStateChange }` 与
  `muxterm_poll_workspace_events(...)`。旧 `muxterm_poll_events` 改为只向消费者返回 active
  workspace event；它仍在 Core 内 poll background 供 Index/attention 使用，但不能拍平后
  外发。两种 API 不能在同一个 platform 实例上并用。
- `CStateChange` 的 size/offset 不变，`window_id` 继续为 0；不得借旧 Window 字段塞 workspace
  hash。`PaneIndexSnapshot` 在 Core 消费后从两种 FFI 输出中过滤。

### 3.3 Target 与 WorkspaceSpec 身份

`TargetConfig` 和 `WorkspaceSpec` 都增加：

```rust
workspace_id: Option<String>
```

规则：

- `path` 始终是用户项目/checkout 路径；
- `workspace_id` 是 runtime candidate 的稳定目标 id；Herdr 为 `wN`；
- `WorkspaceSpec::herdr/ssh_herdr` 必须分别接收 path 与 workspace_id，不再把二者塞入同一字段；
- `TargetConfig.socket` / `WorkspaceSpec.socket` 统一表示 target-side server socket；SSH
  forward 产生的本地临时 socket 只保存在 Driver/Runtime guard 中，不能持久化或参与 id；
- 为兼容现有 `WorkspaceId` 五段序列化，`WorkspaceSpec::id()` 的第五身份段在
  `workspace_id.is_some()` 时使用 workspace_id，否则使用 path；禁止把这个兼容字段反向
  写回 `TargetConfig.path`；
- `WorkspaceId` 的显示/parse/FFI 回归测试必须证明旧 tmux/shell id 不变，Herdr Project 与
  Existing 产生同一 id。

### 3.4 Catalog resolver

```rust
struct ResolvedTarget {
    canonical: TargetConfig,
    spec: WorkspaceSpec,
}

enum ResolveIntent { AttachOnly, CreateIfMissing }

Catalog::resolve_target(&TargetConfig, ResolveIntent) -> Result<ResolvedTarget>
Catalog::open_target(&TargetConfig, ResolveIntent) -> Result<&mut Workspace>
WorkspacePool::open_resolved(ResolvedTarget, runtime) -> Result<&mut Workspace>
```

文件落点锁死：

```text
src/core/catalog/resolver.rs
  ResolvedTarget / ResolveIntent / TargetIdentityKey / ResolutionStage
  SessionCandidate -> TargetConfig
  Catalog::resolve_target / open_target / open_resolved
src/core/catalog/driver.rs
  typed session/socket/workspace_id/project_path candidate fields
src/core/quickconnect/model.rs
  TargetConfig only；不 discovery、不推导 socket
src/core/workspace/workspace.rs
  resolved_target: Option<ResolvedTarget>
```

`SessionCandidate` 目标字段为 `runtime_id/transport_id/target/session/socket/workspace_id/
project_path/name`：Herdr default session 规范成 `"default"`，socket 始终是 target-side，name
必须非空；迁移完消费者后删除无类型 `extra` 与结果侧重复 `namespace`。Driver 的 list
namespace 过滤参数可以保留。

`Workspace` 是 descriptor 的唯一 Pool 内所有者；`PooledWorkspace` 不复制一份，platform
更不能保存 side table。descriptor 在 open 时按完整 value 注入。复用同一 slot 时先比
identity key：不同 key 立即 collision；相同 key 只允许 Core 以整值替换补全原本缺失的
canonical name/path，attach identity 与 WorkspaceId 不得改变。`Catalog::open_resolved` 用
`canonical.name` 作为 Workspace 显示名（resolver 保证非空），不能再把 Herdr named session
误当显示名。

resolver 实现 [`docs/HERDR-RUNTIME-STABILITY.md`](docs/HERDR-RUNTIME-STABILITY.md) §7.2 的
精确 identity→旧格式迁移→唯一 label→selected-local-session create→SSH no-start 优先级。

- `SessionCandidate` 显式携带 session、target-side socket、workspace_id 与可获得的
  project path；Core/Catalog 把它转换为 `TargetConfig`，Linux 不得再按 `$HOME` 推导 socket
  或把无类型 `extra` 当 workspace id；
- Existing、Recent、已保存 Project 使用 `AttachOnly`；只有用户本次明确执行“新建 Project”
  才使用 `CreateIfMissing`。缺 identity、旧格式迁移或普通重连本身绝不授权创建；
- Project、Recent、Existing 都调用 resolver，再调用内部 `Catalog::open_resolved`；低层裸
  `Catalog::open(&WorkspaceSpec)` 不得用于这三个产品入口；
- Catalog 打开成功后在 Workspace 保存 Core-owned `ResolvedTarget`；Pool 不复制第二份；Recent 与当前连接
  高亮读取 `canonical`，不得从 `WorkspaceId` 反向拼 TargetConfig 或在 Linux UiState 建
  身份 side table；
- `ResolvedTarget::identity_key()` 固定比较 transport/target/runtime/session/target-side
  socket/workspace_id；QuickConnect 去重、badge、当前高亮与 Pool 复用不得再用
  `name@transport`。不同 identity key 若碰撞到同一五段 WorkspaceId，必须明确失败；
- identity key 前先统一 Local target=`""`、SSH target=原 Host alias、Herdr 空 session=
  `"default"`；SSH socket 保留 target-side 绝对字符串且不得在本机 canonicalize；缺必要字段
  只能使用 provisional UI key，不能进入 Pool 复用；
- Project 成功后把 resolved session/socket/workspace_id 写回 `quickconnect.toml`；
- platform 删除 `connect_herdr()`、`connect_herdr_ssh()` 和 path-as-id fallback；
- 错误保留 `Discovery/IdentityResolution/WorkspaceCreate/SocketForward/RuntimeConnect` 阶段。

`Catalog::open(&WorkspaceSpec)`/`WorkspacePool::open_spec` 只保留给测试 mock、旧 CLI 或迁移期
内部调用，descriptor 为 None；不得从 platform 的 Project/Recent/Existing 新路径调用。
`Catalog::open_resolved` 是唯一调用 Pool `open_resolved` 的产品门面；Pool 不做 discovery 或
TargetConfig 解析，只负责 descriptor 注入、slot identity/collision 与 Runtime 生命周期。

### 3.5 Herdr adapter 内部对象

至少包含：

```text
StreamMode                 Observe | Control
PaneStreamSlot             pane-keyed ownership + generation + event ordinal + wire seq + retry
ControlIntent              user-intent epoch + takeover suppression + bounded input handoff
PaneStreamEvent            pane + generation + event ordinal；Frame 另带 wire seq
MutationQueue              one in-flight + bounded FIFO；每项使用 dispatch-time topology baseline
PendingMutation            NewTab/SplitPane + expected ids + deadline/probe state
ensure_pane_initialized    唯一新 pane 初始化入口
reconcile_stream_modes     唯一 stream transition 入口
```

`ObserveStream` 可以保留文件名，但 API 必须显式区分 observe/control；若重命名为
`PaneTerminalStream`，只做直接相关引用迁移，不顺手改其它 runtime。

---

## 4. TDD 与提交纪律

每个工作单采用同一流程：

1. 写最小复现测试；
2. 运行该测试并记录预期 RED 的断言与退出码；
3. 写最小实现；
4. 同一测试 GREEN，再跑受影响的既有回归；
5. `cargo fmt`/targeted clippy 通过后，把测试和实现放进一个可独立绿色 commit；
6. 不提交只有失败测试的永久 RED commit；RED 证据保存在实施日志/PR 描述；
7. commit subject/body 全英文，不加 `Co-authored-by`。

若工作单结束时 required gate 仍红，不得为了“按顺序提交”把失败状态称为完成。

---

## 5. W0 — 基线、最小 RED 与诊断字段

### 5.1 目的

冻结真实基线，证明后续失败不是环境或用户默认 session 干扰。此工作单不改行为。

### 5.2 操作

1. 核验分支、HEAD、dirty files、工具版本：

   ```bash
   rtk date -Iseconds
   rtk date
   rtk git status --short --branch
   rtk git rev-parse HEAD
   rtk rustc --version
   rtk cargo --version
   rtk tmux -V
   rtk herdr --version
   rtk env -u HERDR_ENV -u HERDR_SESSION herdr status --json
   ```

2. 只读检查四份日志；不得把大 `.log` 提交进仓库。
3. 对照 `src/core/runtime/herdr/wire.rs` 的 protocol 19 与
   `~/Developer/terminal/herdr/src/protocol/wire.rs` 当前 protocol 20，记录 target 是前者；
   `herdr status --json` 只做 client/server 版本兼容性只读核对，不把用户 default session
   当测试 fixture，也不对它执行任何 mutation。
4. 运行当前最聚焦的 baseline case；本地结果可能因时序/环境为绿，也可能复现远端红，必须
   如实记录，禁止预设结论：

   ```bash
   rtk cargo test --no-default-features --features tui \
     --test herdr_authority_contract \
     local_and_ssh_herdr_focus_and_observer_binding_match_server_authority \
     -- --exact --nocapture --test-threads=1
   ```

5. 记录当前测试到底经过了 API/Core/VTE 哪几层，禁止只记“失败/通过”；即使本地 baseline
   绿色，W2/W5 仍要先增加能确定性触发 stale generation、takeover suppression 与
   response/snapshot 竞态的 RED，不能拿一次本地绿跳过修复。
6. 定义 lifecycle tracing 字段：workspace、product pane、Herdr pane、generation、mode、
   transition、reason、retry、event_ordinal、wire_seq；后续各工作单都复用。

### 5.3 完成证据

- baseline 文本含绝对时间、版本、HEAD、退出码；
- 测试只创建 `muxterm-test-*` named Herdr session/隔离 tmux socket；
- 默认 Herdr session list 前后不变；
- 无 commit，或仅在实施记录中保存证据。

---

## 6. W1 — Core foreground、Index 与释放契约

### 6.1 RED

在下列位置增加纯逻辑测试：

- `src/core/model/backend.rs` / mock runtime：能记录 `set_foreground` 调用；
- `src/core/workspace/pool.rs`：
  - open 第一个 workspace → `[true]`；
  - open/activate 第二个 → 旧 `[false]`、新 `[true]`，不重复 transition；
  - insert_connected 与 activate 同语义；
  - `PersistDetach` runtime close 走 Detach，非持久 runtime 走 Shutdown；
- `src/core/workspace/workspace.rs`：`PaneIndexSnapshot` 替换 Index，后续
  `PaneOutput` 增量追加；它不等价于 `PaneFrame`。

预期 RED：Runtime 没有 foreground API；Pool 没有调用；StateChange 没有新变体。

### 6.2 GREEN

- 增加默认 no-op `Runtime::set_foreground`；
- Workspace 暴露最小转发方法，Pool 是唯一调用者；
- 所有 active/background 转换恰好通知一次；
- `release_runtime` 改为查 `RuntimeCapability::PersistDetach`，删除 runtime 字符串判断；
- 增加 `PaneIndexSnapshot`，Workspace 只更新 PaneBuf；
- 所有 exhaustive match 显式处理新事件；platform 先明确 no-op，W4 再做批处理。

### 6.3 受影响文件

`src/core/model/backend.rs`、`src/core/model/backend/mock.rs`、
`src/core/model/state.rs`、`src/core/workspace/workspace.rs`、
`src/core/workspace/pool.rs`，以及因 exhaustive match 必须调整的直接消费者。

### 6.4 门禁与 commit

```bash
rtk cargo test --lib workspace:: -- --test-threads=1
rtk cargo test --lib model::backend:: -- --test-threads=1
rtk cargo clippy --no-default-features --features tui -- -D warnings
```

建议 commit：`feat(core): expose foreground and index snapshot runtime semantics`

---

## 7. W2 — Pane-keyed generation stream registry

### 7.1 RED：wire 与状态机

在 `src/core/runtime/herdr/observe.rs`、`runtime.rs` 的单测中锁定：

1. observe start 精确发 `ObserveTerminal`，没有 takeover；
2. control start 精确发 `ControlTerminal`；只有显式用户 focus/input promote 可带
   `takeover=true`；
   open/reattach/Pool activate/`set_foreground(true)` 自身的首次尝试必须 `takeover=false`，
   冲突后降 Observe；
3. protocol-19 Hello/Observe/Control 的 exact bytes 与既有 wire fixture 一致；protocol 20
   Welcome 或 mismatch 必须明确拒绝，不能继续按 19 解码；
4. 同 pane 不得同时存在两个 Starting/Live slot；
5. generation N 被 N+1 替换后，N 的 Frame/Error/Closed 全部丢弃；
6. current generation 的 event ordinal 必须递增；TerminalFrame wire seq 重复/倒序丢弃；
   diff seq gap 使 generation 有界失败，full seq gap 允许重建 baseline 并记录诊断；
7. 同一批重复 Error/Closed 只安排一个 retry；
8. 初始/用户 start 不计 retry；自动 retry 精确为 100/200/400/800/1600ms 五次，第五次
   再失败后 Degraded；
9. 重复 snapshot/layout/resize 或同 topology revision 不得重置 retry budget；full baseline
   后连续 Live 10 秒才恢复普通故障预算，短 flap 不恢复；
10. taken-over 的 current control 进入 suppression 并降为 Observe；重复 reconciliation
    即使 desired 仍为 Control，也不得清闩锁或抢 control；新本地 focus edge/input 只建立
    一个新的 control intent；suppression 清除后同一 Starting/Backoff intent 的后续 write
    只入队，不得逐 write 增加 intent 或重启 stream；
11. detach/shutdown 后旧事件不能启动 stream；
12. full 前 diff 最多 256 event/2 MiB；任一溢出使当前 generation 有界失败，generation
    切换清理旧 pending diff。
13. Control Starting 期间 input 最多 256 write/64 KiB，resize latest-wins；队列绑定 control
    intent 并跨同一 intent 的普通自动 retry；握手后 resize 先发、input 按序恰好一次，
    stale/detach/suppression/overflow 不得静默丢键或串 pane。

时间测试必须注入 fake clock/显式 Instant，不允许真实 sleep 1.6 秒。

### 7.2 GREEN：实现 registry

- `observe_streams: Vec<_>`、`observe_initial: HashSet<_>` 替换为
  `HashMap<PaneId, PaneStreamSlot>`；
- reader event 加 generation 与 thread-local event ordinal，并保留 `TerminalFrame.seq`；
- slot 增加 `control_intent_epoch` 与 `SuppressedAfterTakeover`；只有新的本地 focus edge 或
  真实 input 清除 suppression，重复 server snapshot/reconciliation 不清除；
- start/handshake 由 generation-tagged worker 完成，调用线程只登记 Starting；
- start/replace 前先递增 generation，使旧 reader 立即 stale；
- reconciliation 先计算全体 desired mode，再做最小 transition；
- active foreground pane = Control；同 workspace 其它 pane与 background workspace = Observe；
- resize 只发给 current control；向 background pane 输入前先完成产品焦点切换；
- 删除 `rebuild_dead_observe_stream()` 的立即无界重建；
- retry scheduler 每 pane 最多一个 in-flight start；
- control worker 成功后先发 coalesced resize，再 flush intent-bound input queue；自动 retry
  可换 generation 但不得换 intent，demote/detach/suppression/Degraded 产生明确
  input-not-delivered diagnostic；
- current generation full 后连续 Live 10 秒才清零普通故障 retry budget；
-主动 Drop/Detach/replace 的 EOF 不计 retry；
- lifecycle tracing 使用 W0 字段，测试可读取确定性 transition counters。

### 7.3 真实 Herdr contract

扩展 `tests/herdr_authority_contract.rs` 或新增 `tests/herdr_stability_contract.rs`：

- named local 与 loopback SSH 各建 4 tab/至少一个 3 pane；
- 服务端断言只 active pane 有 controller，其余 observer；
- 连续 focus 所有 pane 后，每次最多一个 controller；
- 对同一 pane 制造 takeover/EOF，10 秒内自动 start ≤5；
- 重复 snapshot/reconciliation 后仍为 Observe；
- 用户再次 focus/input 后只 promote 一次并能输出 token；
- fake clock/contract 证明短 flap 不重置预算，连续 Live 10 秒才重置。

若新增 integration target，必须同步 `Cargo.toml`、`scripts/test.sh` 和 workflow path filter。

### 7.4 门禁与 commit

```bash
rtk cargo test --lib core::runtime::herdr -- --test-threads=1
rtk cargo test --no-default-features --features tui \
  --test herdr_authority_contract -- --nocapture --test-threads=1
rtk cargo test --no-default-features --features tui \
  --test herdr_stability_contract -- --nocapture --test-threads=1
```

最后一条只在创建新 target 时运行。建议 commit：
`fix(herdr): isolate pane stream ownership by generation`

---

## 8. W3 — Herdr bootstrap：Index 与 Surface 彻底分离

### 8.1 RED

新增/改写以下测试：

- `pane_read_seeds_index_without_surface_event`：`pane.read` 只产生
  `PaneIndexSnapshot`，不得产生 `PaneFrame/PaneOutput/PaneSnapshot`；
- `full_frame_establishes_generation_surface_baseline`：首个 current-generation full frame
  产生一个 `PaneFrame`；
- `pre_full_diff_replays_only_after_baseline`：full 前 diff 不先画；full 后丢弃更旧 seq，只
  追赶严格连续的更大 wire seq，queue gap 不得越过；
- `full_frame_timeout_after_five_seconds_is_degraded_without_index_fallback`（fake clock）；
- `stale_full_frame_cannot_replace_current_index_or_surface`；
- `generation_change_keeps_old_surface_until_new_full`；
- Workspace 测试证明 Index snapshot 可被搜索，但 platform event collector 不收到 Surface feed。

旧测试 `full_observe_frame_replaces_seed_buffer_before_incremental_bytes` 必须按新数据源语义重写，
不能继续保护 `pane.read → PaneFrame` 的错误行为。

### 8.2 GREEN

- `seed_one_pane()` 改发 `PaneIndexSnapshot`；
- full frame 经 generation/event ordinal/wire seq 过滤后同时：替换当前 Index frame、作为 raw
  `PaneFrame` 交付 Surface；
- diff 作为 `PaneOutput`；
- full timeout 后 slot 进入 Degraded，保留旧像素；禁止 fallback 重播 pane.read；
- 输出 buffer 只保存当前 frame + 后续 diff 的 Index 语义，不把多个 full 永久拼接；
- detach/reattach 不从旧 Runtime channel 接受任何字节。

### 8.3 门禁与 commit

```bash
rtk cargo test --lib core::runtime::herdr::runtime::tests -- --test-threads=1
rtk cargo test --lib core::workspace::workspace::tests -- --test-threads=1
rtk cargo test --no-default-features --features tui \
  --test herdr_feature_contract -- --test-threads=1
```

建议 commit：`fix(herdr): separate index snapshots from surface frames`

---

## 9. W4 — Linux Surface 批处理、follow-tail 与 Ctrl-L 稳定性

### 9.1 RED

先增加不依赖真实 Herdr 的批处理单测/GTK 小测试：

1. 同 batch 为 `PaneAdded + LayoutChanged + PaneResized + PaneFrame + PaneOutput` 时，记录顺序
   必须是结构、一次 refresh、frame、output；
2. `PaneFrame` 必须像 `PaneSnapshot` 一样延迟到结构之后，但不能调用 `seed_snapshot/reset`；
3. `PaneIndexSnapshot` 不调用任何 PaneView feed；
4. 已注册 Surface 的 pane 切 tab 20 轮后 RenderTrace seeds/resets 增量均为 0；
5. baseline 后且用户位于底部时，viewport 跟随底部；用户主动上滚时不强拉；
6. Ctrl-L 后切 tab/resize/Index update 不让 BEFORE token 回到当前屏。
7. 首次 attach 后尚未访问的 hidden tab 也已经有常驻 PaneView；其 Frame/Output 在隐藏期间
   按 `(WorkspaceId, PaneId)` 到达，第一次切入 seeds/resets 增量仍为 0；
8. `WorkspacePool::poll_background()` 的 Surface 事件进入对应 background pixel cache；切回
   workspace 后内容连续，且没有用 PaneBuf/Index reseed。
9. FFI wrapper 测试构造两个 Workspace、各有 `PaneId(1)`，断言新 poll 的 workspace id 与
   raw bytes 一一对应；旧 poll 不泄漏 background event；原 `CStateChange` size/offset 和
   `window_id=0` 保持。

### 9.2 GREEN

- `dispatch_event_batch()` 改为携带 WorkspaceId 的 workspace-aware 四阶段 dispatcher，
  Surface 阶段显式包含 `PaneFrame`；
- topology sync 为该 workspace 所有 tab leaves 建立常驻 PaneView 并设置字符格；active batch
  最多一次可见 mount/chrome refresh，background batch 只更新自己的 pixel cache；
- `PaneFrame`/`PaneOutput` 按 `(WorkspaceId, PaneId)` 对常驻 PaneView raw feed，包含 hidden
  tab 与 background workspace；`poll_background()` 不能只做 attention 后丢字节；
- 增加 workspace-aware additive FFI poll wrapper；旧 poll 只交付 active event，后台事件仍
  在 Core 更新 Index/attention。Linux 直接 Rust dispatcher 与 macOS/FFI wrapper 必须共享
  同一 workspace-tagged batch 语义；
- FFI handle 的 deferred queue 在收取 Pool 结果时就保存 `(WorkspaceId, StateChange)`；wrapper
  字符串/字节指针沿用“下一次 poll/free 前有效”的生命周期。旧 callback 无 workspace 字段，
  只能收到 active event，不能成为 background 的第二条无身份旁路；
- topology 阶段后 Surface 仍缺失属于 lifecycle failure，不得静默丢帧或从 PaneBuf/Index
  fallback；
- 保留 pixel cache，切 tab/workspace 只摘挂 widget，不重建/重播；
- baseline/catch-up 完成后执行一次 follow-tail；后续遵守用户 scroll lock；
- Ctrl-L 只通过真实输入通道，禁止 UI 额外 reset 或 Index reseed；
- 删除/收紧误导性的 `seed_raw from core pane_output` 日志，换成 generation-aware baseline trace。

### 9.3 受影响文件

Core/FFI 子单元：`src/core/protocol/ffi/api.rs`、`types.rs`、macOS CoreBridge 两份同步 header/
Swift bridge，以及最小 ABI 测试。Linux Surface 子单元：`src/platform/linux/window.rs`、
`layout_host.rs`、`pane_view.rs` 和最小测试 helper。不要修改主题、搜索或 attention 业务逻辑，
除非新 StateChange exhaustive match 必需。

### 9.4 门禁与 commit

```bash
rtk cargo test --features gtk --lib dispatch_event_batch -- --test-threads=1
rtk cargo test --lib core::protocol::ffi -- --test-threads=1
rtk xvfb-run -a cargo test --features gtk \
  --test linux_render_e2e -- --test-threads=1
rtk xvfb-run -a cargo test --features gtk \
  --test linux_runtime_transport_matrix_e2e \
  isolated_matrix_child -- --exact --nocapture --test-threads=1
```

子进程 harness 在 W7 完成前，最后一条可先运行对应小场景。本工作单拆成两个独立绿色
commit，不能把 ABI 与 GTK 大改揉在一起：

1. `feat(ffi): preserve workspace identity in event polling`
2. `fix(linux): apply topology before herdr surface frames`

---

## 10. W5 — Tab/Pane mutation 权威收敛与数字名称

### 10.1 RED：纯逻辑

在 Herdr runtime 单测中覆盖：

- `NewTab { name: None }` JSON 完全没有 `label` key；Some 才带；
- snapshot 返回非空 label 时使用权威 label；空/缺失时按 protocol-19 bijective base-32 解码
  public tab id，并显示十进制数字（`tA -> "10"`）；不按 GUI 下标重编号，删除后允许缺口；
  非法 public id 必须 convergence error，不能变成空名或 `0`；
- create response 先到、snapshot 先到两种顺序，最终只发一次 TabAdded/PaneAdded；
- response 包含新 pane 但 snapshot 还没有 layout 时保持 Pending；
- NewTab snapshot 已有 tab/root pane、但 active tab/focused pane 或 Layout.active 仍旧时保持
  Pending；
- split snapshot 有新 leaf 但 `focused_pane_id` 仍旧时保持 Pending；
- snapshot 收敛后新 pane registry 的 current generation 必须 Live 且 full baseline Ready；
- 5 秒 deadline 到期只产生一次阶段化失败，不 busy-loop；
- fake clock 验证 probe 在 dispatched_at 后绝对
  +100/+250/+500/+1000/+2000/+4000ms，且同时至多一个 in-flight、不得越过从 enqueue
  起算的 5 秒 end-to-end deadline；
- 连续提交两个 NewTab/SplitPane 时只有一个 server request in-flight，第二项在第一项收敛
  后以新 topology baseline 派发；32 项 FIFO 满和排队超时均明确失败；
- detach 时 pending worker 的晚响应被 generation/lifecycle guard 丢弃。
- 入队成功返回 `Accepted(operation_id)`，不能返回语义为“已完成”的 Done；最终 Completed
  或各阶段 Failed 只发一次 `MutationSettled`；队列满直接 Rejected，不能先 Accepted 再丢失。
- topology/focus 已收敛但 current-generation full baseline 尚未 Ready 时仍保持 Pending；新
  pane 的 full deadline 取 `min(stream_started_at + 5s, mutation.deadline)`，不能在 enqueue
  起算的 5 秒之后再加一段 5 秒。

### 10.2 GREEN：异步 mutation worker

- `execute(NewTab/SplitPane)` 把请求放入最多 32 项的 mutation FIFO；每个 Runtime 同时只
  派发一个阻塞 socket worker，并在实际派发时记录 baseline；
- 入队返回 `TaskOutcome::Accepted { operation_id }`；同步 `Done` 的既有语义不变；
- topology/focus/current-generation full baseline 全部收敛后发唯一 Completed settlement；任何
  阶段失败发唯一带阶段 Failed settlement，GTK 转成可见通知；
- Core FFI/TUI/Linux/macOS 的 exhaustive mapping 同步支持 Accepted 与 MutationSettled；现有
  C ABI task 返回码可继续把 Accepted 记为请求成功，additive `muxterm_execute_json` 返回
  operation id；`STATE_MUTATION_SETTLED=16` 的既有 data buffer 携带
  operation id/kind/result/stage/reason JSON，不能扩 struct 或吞掉异步失败；
- API response 只填写 expected ids；不再调用 `apply_created_tab/apply_split_pane` 猜最终树；
- pending 在实际派发的同一原子步骤中记录 lifecycle generation、target 和 tabs/panes
  baseline（不能在 enqueue 时记录）；event snapshot
  先到时只接受唯一 topology delta，response 与 delta 不一致时报 convergence error；
- event snapshot 优先，后台 probe 兜底；晚到 response/probe 必须通过 lifecycle guard；
- 所有新 pane 路径统一调用 `ensure_pane_initialized()`；
- `ensure_pane_initialized()` 幂等建立 id mapping、PaneInfo、layout relation、Index seed、
  registry slot、event scope；
- 完成条件严格采用专项设计 §6.4，registry 必须 Live 且 baseline Ready；
- mutation 失败进入 notification/Runtime diagnostic，不吞错、不继续 path-as-id；
- 当前 mutation 完成/失败后才派发 FIFO 下一项；队列等待也受 5 秒 end-to-end deadline；
- 按钮 handler 与快捷键 handler 不主动同步 `refresh_ui()`，只等 StateChange。

### 10.3 真实 Core contract

更新 `herdr_authority_contract`：local/SSH 都必须验证：

- NewTab 后服务端 snapshot、Workspace tab/layout/focus 一致；
- 两次 split 后树为 H(left,V(right-top,right-bottom))；
- active pane 是第二个 created pane；
- 新 pane 输入 token 可从服务端 `pane.read` 与 Workspace 找到；
- tab 默认名称非空，且 Herdr raw 数字 label 与 public tab id 后缀一致。

### 10.4 门禁与 commit

```bash
rtk cargo test --lib core::runtime::herdr::runtime::tests -- --test-threads=1
rtk cargo test --lib core::protocol::ffi -- --test-threads=1
rtk cargo test --no-default-features --features tui \
  --test herdr_authority_contract -- --nocapture --test-threads=1
rtk cargo test --no-default-features --features tui \
  --test runtime_transport_matrix_contract -- --nocapture --test-threads=1
```

建议 commit：`fix(herdr): converge tab and pane mutations from snapshots`

---

## 11. W6 — Project 与 Existing Connection 统一身份

### 11.1 RED：store 与 resolver

必须先增加：

- `TargetConfig` session/socket/workspace_id TOML round-trip；
- 旧 TOML 缺新字段仍可读，保存后迁移；
- `path` 是目录，`workspace_id` 是 `wN`，两者互不覆盖；
- exact identity 命中；
- 旧 path 恰为 workspace id 的一次性迁移；
- 唯一 label/name 命中；
- 同名两候选返回 ambiguity 并列出 id；
- `AttachOnly` 无匹配时不创建；只有 `CreateIfMissing` 且所选 local named/default session
  已运行时可 create，禁止偷偷换 default；若 session/socket 未明确且无法唯一解析则返回
  choice-required/ambiguity，CreateIfMissing 也不能任选 default；SSH 两种 intent 都是无匹配
  即失败且零启动命令；
- `SessionCandidate` 的 session、target-side socket、workspace_id 由 Core 直接转换，Linux
  不按 HOME 猜 socket、不读 `extra`；缺权威 project path 时保持空值或合并同 identity 的
  已保存 Project path，绝不回填 workspace id；
- Discovery/IdentityResolution/WorkspaceCreate/SocketForward/RuntimeConnect 错误阶段保留；
- Existing 与保存后重载的 Project 产生相同 identity key、attach spec 身份字段和
  `WorkspaceId`；存在同 identity Project 元数据时完整 `ResolvedTarget` 也相同；
- QuickConnect 去重/badge/highlight 使用 identity key；同名不同 session/socket 不合并，
  不同 identity key 撞到同一五段 WorkspaceId 时明确失败；
- Catalog open 后 Workspace 保留 canonical descriptor、Pool 不复制，Recent round-trip 与原
  TargetConfig 等价；旧的 `workspace_to_target_config(WorkspaceId)` 路径必须 RED。

Local 与 loopback SSH 都要跑 parity contract。

### 11.2 GREEN：Core 统一入口

- 扩展 TargetConfig/WorkspaceSpec constructors 与 id 计算；
- `Catalog::resolve_target(config, intent)` 成为唯一 TargetConfig→ResolvedTarget resolver；
- ExistingEntry/SessionCandidate 转换在 Core 内完成，保留 transport target、
  session、target-side socket、workspace_id 和可获得的 project path；
- SSH resolver 保留远端 socket path；`HerdrDriver::open` 再创建本地 forward，Runtime
  shutdown 清理它，Project/Recent 永不保存临时 forward 路径；
- Project/Recent/Existing 都调用 Catalog `open_target/open_resolved`，打开后的 Workspace
  保存 Core-owned descriptor，Pool 不复制第二份；
- 删除 Linux `connect_herdr()`、`connect_herdr_ssh()`、手工 forward/runtime 构造；
- 后台连接仍离开 GTK 主线程，但后台任务调用 Core Catalog API；
- 初次新建 Project 才传 `CreateIfMissing`；Existing/Recent/普通 Project 重连传
  `AttachOnly`。resolved Project 成功后原子写回 quickconnect store；
- connection error 的完整 anyhow chain 进 tracing，用户通知显示阶段和身份摘要；
- Recents/当前连接高亮不得从 `WorkspaceId.path` 反推 Project path；删除 platform
  `workspace_to_target_config` 反向构造，改读 Core 保存的 `ResolvedTarget.canonical`。
- `ResolvedTarget`/`ResolveIntent`/identity key/error stage 固定实现在
  `src/core/catalog/resolver.rs`；`SessionCandidate` 的 typed 字段留在
  `src/core/catalog/driver.rs`。禁止把相同 resolver 复制进 Linux/macOS。
- Catalog 打开使用 canonical Project/candidate name 作为 Workspace 显示名；相同 identity 的
  slot 复用只可整值补全 canonical name/path，不能改变 attach identity。

### 11.3 FFI/macOS 边界

- 保留现有 `muxterm_workspace_open(...)` ABI，明确降为 descriptor=None 的低层兼容入口；
- 新增 additive `muxterm_workspace_open_target_json(handle, target_json, intent)`，JSON 携带
  name/runtime/transport/target/path/session/target-side socket/workspace_id，并走 Catalog
  resolver；禁止给旧 C 函数尾部硬加参数；
- `muxterm_workspace_list` 的 JSON 行增加 optional `resolved_target`，旧消费者忽略未知字段；
  Core/Swift round-trip 证明 path/socket/workspace_id 不丢；
- Swift 不解析 Herdr 协议，只消费 Catalog 返回的 candidate/spec；
- 添加 FFI JSON round-trip 与未知字段兼容测试；
- 不在 macOS 写第二套 resolver。

### 11.4 门禁与 commit

```bash
rtk cargo test --lib core::quickconnect -- --test-threads=1
rtk cargo test --lib core::catalog -- --test-threads=1
rtk cargo test --test existing_ssh_contract -- --test-threads=1
rtk cargo test --test herdr_project_identity_contract -- --nocapture --test-threads=1
rtk xvfb-run -a cargo test --features gtk \
  --test linux_existing_e2e -- --test-threads=1
```

若新建 `herdr_project_identity_contract`，同步注册与门禁。建议 commit：
`fix(catalog): resolve saved herdr targets canonically`

---

## 12. W7 — 真实 GTK/VTE 子进程矩阵

专项场景和阈值以 [`docs/HERDR-TESTING.md`](docs/HERDR-TESTING.md) §3–§5 为准。

### 12.1 先修 harness，而不是先改断言

重构 `tests/linux_runtime_transport_matrix_e2e.rs`：

```text
parent
  -> runtime_list + transport_list + accepted_transports
  -> required cells: tmux/herdr × local/loopback-ssh
  -> 每个 cell × scenario 启动 current_exe child
child
  -> 初始化一次 GTK
  -> 创建一个 AppWindow
  -> 创建一套独立 fixture
  -> 只执行一个 scenario
  -> shutdown/cleanup/exit
```

要求：

- 父进程不初始化 GTK；
- child 用 `MUXTERM_TEST_CHILD/RUNTIME/TRANSPORT/SCENARIO`；
- child args 固定 `--exact isolated_matrix_child --nocapture --test-threads=1`；
- 普通 child 硬 timeout 15 秒，large-history/takeover 30 秒，parent 总预算 15 分钟；
- timeout/失败输出 cell/scenario、stdout/stderr 和 artifact 路径；parent 用预分配的精确
  named Herdr session/tmux socket 做兜底清理，不能依赖被 kill child 的 Drop；
- 新增 runtime/transport 若声明 accepted 但无 fixture，parent 必须失败；
- 非 accepted 组合由 Catalog rejection test 覆盖，不当作 required success cell；
- shell 既有 coverage 保留为独立 contract，但不能替代四格。

### 12.2 禁止测试旁路

本轮场景中禁止：

- `test_handle_action(Action::NewTab/NewPane/...)`；
- `test_poll_once()` 代替生产 16ms GLib timer；
- 手工调用 `refresh_ui()`；
- 只断言 Core 或 Herdr API，不看目标 VTE；
- 只 `contains(token)`，不检查唯一 pane/恰好一次/几何/cursor；
- 多个 AppWindow 在同一进程串行运行。

允许只读测试 helper：获取 widget、VTE 文本、RenderTrace、layout、grid、cursor、transition
counters。触发动作必须使用真实 `clicked`、`EventControllerKey` 和 VTE commit。

所有内容 token 必须能放进单个 visual row，且发送的 shell 命令不得连续包含完整 token
（拆片段后 `printf` 或 fixture 关闭 echo）；这样“VTE 恰好一次”不会把命令回显与程序输出
混为一谈，也不能用放宽到 `contains` 掩盖重复 feed。

### 12.3 必跑 child scenarios

| scenario | cell | 生产入口与核心断言 |
|---|---|---|
| `large_history_100k/393k/500k` | Herdr local+SSH | attach 前生成历史；20 轮切 tab；token 恰好一份；cursor 最后两行；切换后 seeds/resets +0 |
| `new_tab_button` | 四格 | `muxterm-new-tab` clicked；5 秒服务端/Core/GTK 收敛；新 VTE 可输入 |
| `new_tab_shortcut` | 四格 | 真实 Alt+T；通用断言为顺序标签非空且新 VTE 可输入；Herdr 两格额外要求 raw 数字 label、payload 无空 label；tmux raw name 保持自身语义 |
| `split_shortcuts` | 四格 | 真实 Alt+S/Alt+V；权威 focus/layout；三个唯一 VTE |
| `ctrl_l_stays_clear` | 四格 | 真实 Ctrl-L；BEFORE 当前屏消失、AFTER 可见；切换后不复活 |
| `pool_foreground_switch` | 四格 | 两 workspace 切换；Herdr background 只 observe；四格 background Surface 都持续接收；切回内容连续且无 reseed |
| `detach_reattach` | tmux/Herdr 四格 | 丢旧 Runtime 后重连；服务端/Core/VTE 连续；无 stale event |
| `takeover_watchdog` | Herdr local+SSH | 10 秒 automatic retry start≤5；taken-over 后 control auto-start=0；GLib watchdog 最大延迟≤500ms；显式输入只 promote 一次 |
| `project_existing_parity` | Herdr local+SSH | 真实 Existing row 与 Project row 分别 click；保存/重载后 identity key、attach spec identity、id/workspace 相同；同 identity Project 元数据合并后完整 resolved target 相同；Core Recent 再开不丢 path/socket/workspace_id |

按钮和快捷键必须是不同 child，不能一个入口成功后替另一个打勾。

### 12.4 RED→GREEN 次序

场景可在对应行为工作单中逐步加，但 W7 最终必须集中执行完整 parent。若 harness 改造暴露
已有 SIGSEGV/全局状态泄漏，先保留 child 失败证据，再修 lifecycle；不得恢复单 AppWindow
循环来“稳定”测试。

### 12.5 门禁与 commit

```bash
rtk xvfb-run -a env G_DEBUG=fatal-criticals GTK_A11Y=none \
  cargo test --features gtk --test linux_runtime_transport_matrix_e2e \
  -- --nocapture --test-threads=1
```

建议 commit：`test(herdr): cover isolated production tab and pane workflows`

---

## 13. W8 — 本地/CI 环境与单入口对齐

### 13.1 固定版本

增加仓库工具链声明并让本地/CI 都校验：

| 工具 | required |
|---|---|
| Rust | 1.97.1 |
| cargo | 1.97.1 |
| tmux | 3.7c |
| Herdr | 0.8.0，protocol 19 |

官方版本源与 release metadata（最终核验于 `2026-08-22T19:31:35+08:00`）：

- Rust 1.97.1：[`channel-rust-1.97.1.toml.sha256`](https://static.rust-lang.org/dist/channel-rust-1.97.1.toml.sha256)；
- tmux 3.7c：[`tmux/tmux` release 3.7c](https://github.com/tmux/tmux/releases/tag/3.7c)；
- Herdr 0.8.0：[`herdrdev/herdr` release v0.8.0](https://github.com/herdrdev/herdr/releases/tag/v0.8.0)。

固定 SHA-256：

| artifact | SHA-256 |
|---|---|
| `channel-rust-1.97.1.toml` | `03569b1886ceb5c05276b50c8431ab111de944cd6140fe1fa7d821dd8e0f29cf` |
| `tmux-3.7c.tar.gz` | `7c60cae9a0e25288e2e24750aafc9e8800fc7fd4555e447e1b29ee4201cfb3bf` |
| `herdr-linux-x86_64` | `b872ea7e40fa2cb17e857ac9b62b1bf26db7b403c622f5d2f3f5b35f6e9acd28` |
| `herdr-linux-aarch64` | `f647ac66468d9efbc642fe534fb284468f0aea60641606fc008dfc0d82a3ca87` |
| `herdr-macos-x86_64` | `77cb5afd6c8fcaaaf3bc28e474ec01c209331ad08094e20d7f8aa9b0bb78d649` |
| `herdr-macos-aarch64` | `d53a9f93fccfdfcc55632927bf51002f5add0aa7990bcdf508ffbd84ac658178` |

具体动作：

- 增加 `rust-toolchain.toml` 固定 channel `1.97.1`；workflow 删除漂移的 `@stable` override，
  或把 Rust setup action 的 toolchain 参数也显式写成 `1.97.1`，随后精确校验
  `rustc/cargo 1.97.1`；
- `scripts/ci/setup.sh` 的 tmux 3.7b 改为官方 3.7c tarball，Linux/macOS 安装后精确
  `tmux -V == tmux 3.7c`；
- CI 临时 runner 上 Herdr 已存在但版本不匹配时必须安装固定资产或失败，不能只判断
  command 存在；开发机只读 preflight 失败，不自动覆盖用户二进制；
- 固定下载 URL 与 checksum；setup 完成打印实际版本；
- 测试开头校验 Herdr protocol 19，版本不符是 required failure。

`scripts/ci/setup.sh` 只负责 CI/明确 opt-in 的 runner provisioning；非 CI 环境若没有显式
`MUXTERM_ALLOW_TOOL_INSTALL=1`，不得执行 sudo/brew/install。新增
`scripts/test.sh doctor` 作为本地只读入口，校验同一版本、locale、DISPLAY/Xvfb、ssh/sshd
能力并输出机器可对比清单。

### 13.2 单一测试入口

`scripts/test.sh` 是命令清单唯一来源：

- `doctor`：只读环境/版本/fixture capability 预检，不安装工具；
- `run core`：fmt、clippy、core/FFI、L0/L1、loopback SSH；
- `run linux`：check/clippy、一次 Xvfb、全部 GTK child matrix；
- `run macos`：Rust FFI、macOS e2e、patch、Swift test；
- 如保留 four-mode 独立 job，增加脚本子命令承载 filter，workflow 不再复制 cargo 命令。

workflow 调整：

- Linux workflow 调 `bash scripts/test.sh run linux`，外层删除第二个 `xvfb-run`；
- Core workflow继续调 `bash scripts/test.sh run core`；
- macOS workflow 调 `bash scripts/test.sh run macos`，删除另一份手写命令序列；
- four-mode job 也必须使用 `rust-toolchain.toml` 和同一 `scripts/ci/setup.sh`，禁止继续安装
  runner apt 的漂移 tmux 或单独使用 `@stable`；
- macOS 启动用户态随机端口 loopback sshd，两个当前 skip 变 required；
- 所有 job 通过同一环境 helper 固定并预检 `LC_ALL=en_US.UTF-8`、
  `LANG=en_US.UTF-8`、`TERM=xterm-256color`、`COLORTERM=truecolor`、
  `RUST_BACKTRACE=1`；locale 不存在时 setup
  必须失败，不能只输出 warning 后继续；
- 打印 uname、Rust、tmux、Herdr、GTK/VTE、ssh、locale、TERM、DISPLAY；
- Core/Linux/macOS path filters 包含新增 tests、toolchain、scripts 与设计涉及文件。

### 13.3 macOS 当前失败专项

- `PaletteSessionListE2ETests` 创建隔离 `-L muxterm-test-*` 后必须等待 readiness，再启动/刷新
  app，不用固定 sleep；
- discovery 必须显式收到同一个 local socket override；
- `PaletteSessionListE2ETests` 的 SSH case 与 `SshAttachE2ETests` 使用 setup 产生的 loopback
  env，不允许 skip；
- 失败 artifact 包含隔离 tmux list-sessions、app log 和 sshd log，不读取默认 server。

### 13.4 CI artifact

每个失败 child 上传专项设计 §9 的日志；加 `if: failure()`，私钥和用户默认 session 内容不
得进入 artifact。

### 13.5 本地门禁与 commit

```bash
rtk bash scripts/test.sh doctor
rtk bash scripts/test.sh run core
rtk bash scripts/test.sh run linux
```

Linux 本机不能替代 macOS；最终需 PR required run。建议 commit：
`ci: align local and remote runtime test environments`

---

## 14. W9 — 全量回归、真实 dogfood 与完成判定

### 14.1 静态/单元门禁

```bash
rtk cargo fmt --all -- --check
rtk cargo check --all-targets --all-features
rtk cargo clippy --all-targets --all-features -- -D warnings
rtk bash scripts/test.sh run core
```

### 14.2 Linux required

```bash
rtk bash scripts/test.sh run linux
```

脚本内部负责唯一一层 Xvfb；命令外不得再套 Xvfb。

### 14.3 隔离手工验收

只用 disposable fixture：

- Herdr named session `muxterm-test-<pid>-manual`；
- tmux socket `-L muxterm-test-<pid>-manual`；
- 4 tab、至少一个 3 pane、三档大历史；
- 逐项真实操作 `+`、Alt+T、Alt+S/V、20 轮切换、Ctrl-L、detach/attach；
- 保存一个 Project，从 Existing 与 Project 各连一次并核对同一 workspace；
- 记录截图/日志/transition counters；
- 清理只清自己的 named fixture，前后默认 session list 不变。

人工检查用于发现视觉问题，不替代自动化门禁。

### 14.4 PR/CI

只有用户另行授权 push 时才更新远端。最终完成要求：

- Core required 绿；
- Linux required 绿；
- macOS required 绿；
- four-mode required 仍绿；
- 无新增 ignore/skip，无“环境不可用所以成功”；
- artifact 中没有用户 session 数据。

---

## 15. 建议 commit 序列

| 顺序 | commit | 主要内容 | 必须绿色的最小门禁 |
|---|---|---|---|
| 1 | `feat(core): expose foreground and index snapshot runtime semantics` | W1 公共 API/Pool/Index | core workspace/backend |
| 2 | `fix(herdr): isolate pane stream ownership by generation` | W2 mode/registry/stale/backoff | Herdr unit + authority contract |
| 3 | `fix(herdr): separate index snapshots from surface frames` | W3 bootstrap/full/diff | Herdr + Workspace tests |
| 4 | `feat(ffi): preserve workspace identity in event polling` | W4 additive wrapper/active-only legacy poll | FFI ABI + two-workspace routing |
| 5 | `fix(linux): apply topology before herdr surface frames` | W4 batch/follow-tail/Ctrl-L 基础 | Linux render targeted |
| 6 | `fix(herdr): converge tab and pane mutations from snapshots` | W5 async mutation/数字名/focus | authority + core matrix |
| 7 | `fix(catalog): resolve saved herdr targets canonically` | W6 identity/resolver/store | catalog/existing/project parity |
| 8 | `test(herdr): cover isolated production tab and pane workflows` | W7 child matrix/真实入口 | GTK four-cell matrix |
| 9 | `ci: align local and remote runtime test environments` | W8 pin/脚本/workflow/artifact | local core+linux；远端三平台 |

每个 commit 只包含本行逻辑和测试。若 W4/W5 的 GTK RED 必须先随实现落地，可将相关场景放进
同一个行为 commit，W7 只做 harness 汇总；不要为了完全匹配表格制造无意义拆分。

---

## 16. 完成检查表

### 16.1 行为

- [x] tab1/tab2/其它 tab 在三档大历史下都显示当前帧，cursor 在底部附近（本地 Herdr
      `large_history_*` child 已通过）；
- [x] 20 轮 tab/pane 切换不 seed/reset，不乱码（本地 matrix/Surface contract 已通过）；
- [x] 从未访问的 hidden tab 与 background workspace 在隐藏期间持续 feed，第一次切入也不
      reseed；
- [x] Ctrl-L 后旧屏不因切换/resize/reconnect 复活；
- [x] `+` 与 Alt+T 分别能创建可输入的新 tab；
- [x] 所有 Runtime 的 tab 顺序标签非空；Herdr 默认 raw tab 数字与权威 public id 后缀一致；
- [x] Alt+S/Alt+V 后服务端/Core/GTK layout/focus 完全一致；
- [x] takeover 风暴有界，UI watchdog 达标，无 CPU busy-loop；
- [x] detach/reattach 后服务端、Index、VTE 都连续；
- [x] Project 与 Existing Connection 打开同一 Herdr workspace。

### 16.2 架构

- [x] 一个 pane 一个 registry slot；stale generation 零副作用；
- [x] 只有 foreground active pane control，其余 observe；
- [x] `pane.read` 永不进入 Surface；
- [x] platform 无 Herdr protocol/identity fallback；
- [x] GUI 无 `if runtime == "herdr"`；
- [x] Pool 依 capability detach/shutdown；
- [x] mutation 只由权威 snapshot 宣告完成。

### 16.3 测试与 CI

- [x] L0/L1/L2/L3 本地门禁全部满足 [`docs/HERDR-TESTING.md`](docs/HERDR-TESTING.md)；
- [x] tmux/herdr × local/loopback SSH 四格本地 required 场景通过；
- [x] 每场景独立 AppWindow process；
- [x] 真实 button/key controller/GLib 主循环；
- [x] Rust 1.97.1、tmux 3.7c、Herdr 0.8.0/protocol 19；
- [x] 本地和 CI 使用同一 `scripts/test.sh`；
- [ ] macOS local list 绿、两个 SSH case 不 skip；
- [ ] PR #20 三个平台 required 全绿（获准 push 后）。

### 16.4 安全与交付

- [x] 默认 tmux/Herdr session 前后不变；
- [x] 未修改 Herdr/herdrm；
- [x] 无大日志、私钥、target/build 产物进入 Git；
- [x] 无新增 ad-hoc cache/temp 目录；
- [x] 用户原有 `scripts/test.sh` 改动被保留；
- [x] 每个 commit 英文、无 Co-authored-by；
- [x] 未经授权不 push、不改 PR。

---

## 17. 实现 agent 的最终汇报格式

最终回复必须分开报告：

1. **实现结果**：对应 W1–W8 哪些完成；
2. **根因与修复映射**：四份日志分别被哪些 test 抓住；
3. **测试证据**：每条命令、退出码、passed/failed/ignored/skipped；
4. **矩阵证据**：四格逐 cell/scenario，不只汇总“matrix passed”；
5. **CI 证据**：Core/Linux/macOS/four-mode run URL；
6. **安全证据**：默认 session 未变、fixture 已清理；
7. **未验证边界**：例如任意真实外部 SSH Host；
8. **commits 与工作树**：hash、subject、剩余 dirty files；
9. **禁止措辞**：只要任一 required gate 红，就不能写“Herdr runtime 已完成”。

---

## 18. 2026-08-24 事故增补（W10）

### 18.1 新事故边界

`test_2026-0824-1856.log` 的确定边界是：已有 Herdr workspace attach，收到约 223KB
baseline，`SplitPane` 被接受，新 pane `w2:p1C` 启动 Observe 后进程终止。当前没有 core dump
或 journal，因此不能把某个 native/GTK/Rust 假设写成已证实的唯一根因。

本轮修复围绕三个已确认违反契约的机制：

- structural event 不能在批次中 inline refresh GTK topology；
- VTE callback 不能通过裸指针重新借用 `UiState` 或按 active workspace 路由输入；
- mutation 的 expected focus 必须先于 Herdr stream mode reconciliation 生效。

### 18.2 已落地 GREEN 单元

- `5fbc2a5`：topology batch coalescing、workspace/pane-owned Surface input FIFO、Herdr
  pending focus mode；新增对应 lib regression。
- `7acc995`：registry-driven runtime contract 增加 attach 后 H/V split、NewTab、逐 pane
  echo；GTK child 增加生产 Existing Connections → attach → split → NewTab → echo 场景。
- `runtime_transport_matrix_contract` 当前 6 格通过（隔离 tmux/Herdr、local/loopback SSH）。

### 18.3 W10 当前执行结果与剩余外部门禁

本地 W10 RED→GREEN 已完成：

1. `attach_then_mutate_existing` 在 tmux/Herdr × local/loopback SSH 四格由独立 GTK child
   跑通；每格覆盖 Existing row attach、Alt+S、Alt+V、`+`、echo、geometry 与 VTE 归属。
2. Herdr-local `herdr_attach_split_incident`（约 223KB baseline）在
   `G_DEBUG=fatal-criticals` + Xvfb 下正常退出；没有 SIGABRT/SIGSEGV/timeout。
3. `zz_probe_ps1`、`zz_probe_reattach` 的有效诊断已有永久 `herdr_direct_reattach` 回归，
   临时 probe 不再由 `scripts/test.sh run linux` 作为 required target。
4. Linux agent target 改为每个场景独立 child process，避免 GTK/VTE allocator finalize
   跨场景污染；5 个 agent 场景全部通过。
5. readiness 单元覆盖 capture 未完成暂存、probe ack、两轮 output、probe retry、pane close
   清理；tmux backend 当前 65 个测试通过。

仍需外部环境完成的只有：本轮不 push，因此尚未产生新的 Core/Linux/macOS PR run；macOS
Swift/Xcode gate 也无法在 Linux 工作区执行。push 获准后按 §16.3 监控 required jobs，不能
把本地绿色替代远端 CI 证据。

### 18.4 2026-08-25 本地门禁记录

- `cargo fmt --all -- --check`：exit 0。
- `cargo check --all-targets --all-features`：exit 0。
- `cargo clippy --all-targets --all-features -- -D warnings`：exit 0。
- `cargo test --lib core::runtime::tmux::backend::tests -- --nocapture`：65 passed。
- `MUXTERM_TEST_THREADS=1 scripts/test.sh run core`：exit 0；lib/main、core 集成、四格
  runtime contract 与 ignored SSH four-mode 复跑均通过。
- `MUXTERM_TEST_THREADS=1 scripts/test.sh run linux`：exit 0；完整 GTK target、Herdr agent
  child 场景、runtime transport matrix（2 tests，119.31s）均通过。
- 直接复现：tmux local `attach_then_mutate_existing` exit 0；Herdr local
  `herdr_attach_split_incident` exit 0。

命令均使用隔离 `muxterm-test-*` tmux socket/named Herdr session；默认 server/session 只读
检查，未执行破坏性操作。

### 18.5 W10 覆盖停止规则

每个生命周期转换只保留一个 L0 contract；每个 registry runtime×transport cell 运行同一
canonical workflow；GTK 只保留生产 attach 与 reattach 两条入口；仅当出现新的 ordering、
generation 或 payload invariant 时新增定向 regression，不再做入口/操作/历史大小/transport
全笛卡尔积。

## 18.6 2026-08-25 旧 PR required failure 对照

本次只读读取了旧 PR #20 的 merge commit `7deb69f`（尚未包含本轮 W10 commits），失败原因
分别是：

| Job | 旧日志中的确定失败 | 当前 HEAD 的对应修复/边界 |
|---|---|---|
| Core `97371893614` | `cargo fmt --all -- --check` 在 `tests/zz_probe_ps1.rs` 与 `tests/zz_probe_reattach.rs` 失败 | `80292fa` 提交了仅格式化的 probe 修复；用户在 probe 中的行为诊断改动仍保持 unstaged |
| Linux `97371892812` | `linux_herdr_agent_e2e` 的 `herdr_agent_done_attention` 以 `corrupted double-linked list` / SIGABRT 结束 | `98c4966` 将每个 agent contract 放入独立 child process；本地完整 Linux gate 已通过 |
| macOS `97371893023` | `PaletteSessionListE2ETests.testLocalSessionListRefreshesBeyondNewSession` 只得到 `titles=["New"]`，另有 SSH case 因环境缺失 skip | `ad2c222` 保留 attach socket、按 request generation 丢弃旧 completion；`20177f0`/`ddce25c` 补齐 loopback sshd 与失败 artifact；Linux 无法运行 Swift/Xcode gate |

这些对照只证明旧 run 的失败点已被当前代码覆盖或有明确本地证据，不能把旧 run 改写成当前
HEAD 的绿色结果。当前工作树未 push，因此没有新的 PR run；获得授权后必须重新跑 Core、Linux、
macOS required，并单独记录四格 aggregate。

## 18.7 2026-08-25 macOS SSH gate 补齐

旧 macOS workflow 没有调用 `scripts/ci/setup-sshd.sh`，因此 Swift 的 loopback SSH
session-list 用例会因缺少 `MUXTERM_TEST_SSH_*` 环境而 skip。`20177f0` 在 macOS required
job 中启动同一 named loopback fixture、写入 `$GITHUB_ENV`，并在 `always()` cleanup 中按
`MUXTERM_SSHD_TMP_DIR/sshd.pid` 清理；`752ec18` 在 Swift 前验证 key/port，`ddce25c` 在
清理前保留失败 sshd artifact。这样
workflow 与 Linux/本地的 SSH fixture 契约一致；
是否在 macOS runner 实际通过，仍须新的远端 run 证明。
