# TmuxBackend 修复 Round 2: tab 切换 + pty 输出显示

## Bug 2: Alt+数字切 tab 不工作（TUI）

### 现象
- 连接 2 tab 的 tmux session，TUI 显示 tab2（2:tmux）
- 按 Alt+1 想切到 tab1，但 tab 栏没变，pane 没变
- 根因：TmuxBackend 的 window/tab id 映射问题

### 分析
- TUI key_event_to_task: Alt+1 → Task::SwitchWindow { target: WindowId(1) }
- 但 TmuxBackend 的 window id 是 tmux 的 @0/@1（从 0 开始），不是 1/2
- 需要确认：muxterm WindowId 和 TabId 的编号规则
  - muxterm WindowId 从 1 开始（LocalBackend 是 1,2,3...）
  - tmux window @N 从 0 开始
  - TmuxBackend 映射时需要 +1 或建立映射表

### 修复要求
1. TmuxBackend::execute 处理 SwitchWindow 时，把 muxterm WindowId 转成 tmux window index
2. 发送  命令到 tmux
3. tmux 的 %window-changed 通知要更新 active tab/window
4. TUI 收到 StateChange::ActiveTabChanged 后重绘
5. 测试：Alt+1 切到 tab1，Alt+2 切到 tab2

## Bug 4: pty 输出不显示（TUI）

### 现象
- TUI 连接 tmux session 后，输入 echo hello tmux + Enter
- shell 的输出（hello tmux）不显示在 TUI 内容区
- TUI 内容区完全空白

### 分析
- TmuxBackend 收到 %output @N content 通知
- 需要确认：是否把 content 追加到 pane 的 output 缓冲
- TUI render_frame 用 state.pane_output(pid) 读输出
- 可能问题：
  a. %output 的 pane id (@N) 和 TerminalModel 的 PaneId 不匹配
  b. output 追加到了错误的 pane
  c. TUI refresh() 没拉到 PaneOutput 事件
  d. pane_output 的数据有但 render_frame 没正确渲染

### 修复要求
1. 确保 %output 的 content 解码后追加到正确 pane 的 output 缓冲
2. 推 StateChange::PaneOutput 事件
3. TUI refresh() 拉到事件后重绘
4. 测试：send-keys echo hello → capture-pane 能看到输出

## 测试要求

### 正向测试
- 2 tab session，Alt+1 切 tab1，Alt+2 切 tab2，pane 正确切换
- send-keys echo hello + Enter → TUI 显示输出

### 反向测试
- Alt+9（不存在的 tab）→ 不 crash，忽略
- 连接只有 1 个 tab 的 session → Alt+1 能工作

### 边界
- tab 切换后 layout 正确更新
- 连续快速切 tab 不丢失事件

## 约束
- cargo test --no-default-features --features tui 必须全过
- cargo fmt + clippy
- commit: fix(tmux):
- 不要 Co-authored-by
