# TmuxBackend 修复 Round 5: attach 模式 + 测试整理

## Bug 7: CLI/TUI 连接 tmux 时只能 new-session，不能 attach 已有 session

### 现象
- muxterm list-sessions -L muxterm-demo 返回新创建的 session，不是已有的 demo session
- muxterm --tui -L muxterm-demo 也创建新 session，看不到已有的 2 tab 3 pane 布局
- Alt+1 切 tab 没反应（因为新 session 只有 1 个 window）

### 根因
- TmuxBackend::connect() 总是用 tmux -CC new-session 创建新 session
- 没有 attach 已有 session 的逻辑
- CLI 和 TUI 都走 connect()，都创建新 session

### 修复要求
1. TmuxBackend 支持两种连接模式：
   - new 模式（默认）：tmux -CC new-session 创建新 session
   - attach 模式：tmux -CC attach-session -t name 连接已有 session
2. CLI attach-session -t name -L socket 命令实现：
   - 用 TmuxBackend attach 到已有 session
   - 查询并返回 session 的 window/pane/layout 数据
3. CLI list-sessions -L socket 应该列出 tmux server 上所有 session（不只自己创建的）
4. TUI -L socket 应该能选择 new 还是 attach：
   - muxterm --tui -L socket → new session（当前行为）
   - muxterm --tui -L socket attach -t name → attach 已有 session
   - 或者：如果只有一个 session 且不是自己创建的，自动 attach
5. attach 后的 pane/window/layout 数据正确同步

### 测试场景
- 用原生 tmux 创建 2 tab 3 pane
- CLI attach 查询：list-sessions, attach-session, list-windows, list-panes, list-layout
- TUI attach：Alt+N 切 tab，pane 正确显示

## 测试整理要求

### 检查现有测试覆盖
1. 列出所有现有测试（tests/ 目录 + src/ 内 cfg(test)）
2. 确认覆盖：
   - LocalBackend: new-session, split, list, kill OK
   - TmuxBackend: new-session, split, list OK
   - TmuxBackend: attach 已有 session MISSING 需要加
   - CLI -L 模式: list, split, new-window OK
   - CLI -L attach 模式 MISSING 需要加
   - TUI -L 模式: tab 切换, pane 显示 PARTIAL 需要加
   - attach/detach 流程 MISSING 需要加
3. 补充缺失的测试

### 新增测试
- attach 到已有 session 正确显示 window/pane/layout
- attach 后 split-pane 原生 tmux 验证
- detach 后 session 持续存在 re-attach 布局保持
- list-sessions 列出所有 tmux session
- TUI attach Alt+N 切 tab pane 正确显示
- 边界：attach 不存在的 session 错误处理
- 边界：attach 空 session（0 pane）不 panic

## 约束
- cargo test --no-default-features --features tui 必须全过
- cargo fmt + clippy -- -D warnings
- commit: fix(tmux): / feat(tmux): / test(tmux):
- 不要 Co-authored-by