# TmuxBackend 修复 Round 3: 切 tab 后 pane 数据丢失

## 问题

### 现象
1. TUI 连接 2 tab 3 pane 的 tmux session，初始在 tab2（1 pane），pty 输出正常显示
2. 按 Alt+1 切到 tab1 → 显示 "0 panes (no pane)"，应该显示 3 pane 嵌套布局
3. 按 Alt+2 切回 tab2 → 也变成 "0 panes"，之前正常显示的 pane 丢了
4. 切 tab 后输入 echo hello 无反应

### 根因推测
- SwitchWindow 命令发送到 tmux 后，tmux 回推 %window-changed 通知
- TmuxBackend 处理 %window-changed 时可能清掉了 pane 数据，没有重新查询目标 tab 的 pane/layout
- 或者 SwitchWindow 映射的 tmux window index 不对，tmux 切到了不存在的 window

### 调试方法
1. 在 TmuxBackend::execute 的 SwitchWindow 分支加日志，看发送了什么 tmux 命令
2. 在 handle_message 的 %window-changed 处理加日志，看收到了什么
3. 对比原生 tmux list-panes 确认 tmux 侧的 window/pane 状态

### 修复要求
1. SwitchWindow 时：
   - 把 muxterm WindowId(N) 映射到正确的 tmux window index
   - 发送 select-window -t <index> 命令
   - tmux 回推 %window-changed 后，重新查询目标 window 的 pane 和 layout
   - 不要清掉已有 pane 数据，而是更新 active tab 指向

2. 切 tab 后：
   - active_tab 正确切换到目标 tab
   - State::panes(active_tab) 返回目标 tab 的 pane 列表
   - State::layout(active_tab) 返回目标 tab 的 layout 树
   - pty 输出继续显示（%output 持续同步）

3. 关键：connect 时应该已经获取了所有 window/tab 的 pane 数据（不只 active tab）
   - 切 tab 只是切换 active 指针，不需要重新查询
   - 如果 connect 时只查了 active tab 的 pane，需要改为查所有 tab

## 测试要求

### 正向测试
- 2 tab session：tab1 有 3 pane（嵌套），tab2 有 1 pane
- 初始连接在 tab2：显示 1 pane，pty 输出正常
- Alt+1 切到 tab1：显示 3 pane，嵌套布局正确
- Alt+2 切回 tab2：显示 1 pane，pty 输出正常
- 输入 echo hello + Enter：输出显示

### 反向测试
- Alt+9（不存在的 tab）：不 crash，不切
- 单 tab session 切 tab：不 crash

### 边界
- 连续快速切 tab：不丢 pane 数据
- 切 tab 后 split-pane：新 pane 在正确的 tab

## 约束
- cargo test --no-default-features --features tui 必须全过
- cargo fmt + clippy
- commit: fix(tmux):
- 不要 Co-authored-by
